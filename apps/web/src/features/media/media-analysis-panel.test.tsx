import { act, fireEvent, render, screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";
import { MediaAnalysisPanel } from "./MediaAnalysisPanel";

const mocks = vi.hoisted(() => ({ mediaAnalysisPreview: vi.fn().mockResolvedValue({ analysis_id: "analysis_1", preview: { duration_sec: 1 } }) }));
vi.mock("../../api", () => ({ api: { listMediaAnalysis: vi.fn().mockResolvedValue({ items: [{ analysis_id: "analysis_1", asset_id: "asset_1", status: "needs_review", progress_percent: 100, queue_position: null, error_code: null, error_message: null, result: null, created_at: "now" }] }), cancelMediaAnalysis: vi.fn(), retryMediaAnalysis: vi.fn(), reviewMediaAnalysis: vi.fn(), mediaAnalysisPreview: mocks.mediaAnalysisPreview } }));
describe("MediaAnalysisPanel", () => { it("shows review state and loads preview before selecting the asset", async () => { const onSelectAsset = vi.fn(); render(<MediaAnalysisPanel projectId="project_1" onSelectAsset={onSelectAsset} />); expect(await screen.findByText(/needs_review/)).toBeInTheDocument(); expect(screen.getByLabelText("asset_1 수동 태그")).toBeInTheDocument(); await act(async () => { fireEvent.click(screen.getByRole("button", { name: "미리보기" })); }); await vi.waitFor(() => expect(mocks.mediaAnalysisPreview).toHaveBeenCalledWith("project_1", "asset_1")); expect(onSelectAsset).toHaveBeenCalledWith("asset_1"); expect(screen.getByText("분석 미리보기 · 1초")).toBeInTheDocument(); await act(async () => { fireEvent.click(screen.getByRole("button", { name: "새로고침" })); }); expect(screen.queryByText("분석 미리보기 · 1초")).not.toBeInTheDocument(); }); });
