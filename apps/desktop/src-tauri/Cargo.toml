[package]
name = "videobox-desktop"
version = "0.1.0"
description = "VideoBox native window shell — points at the owner-ready container stack."
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "videobox-desktop"
path = "src/main.rs"
