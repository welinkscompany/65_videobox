platform_toolsets:
  cli: []
known_plugin_toolsets:
  cli:
    - kanban
agent:
  disabled_toolsets:
    - kanban
context:
  engine: compressor
mcp_servers: {}
# 유진의 두뇌는 이 컴퓨터에서 도는 LM Studio 다. provider 키가 없으면 Hermes 가
# 로그인된 OAuth 제공자로 조용히 넘어가므로 반드시 함께 적는다. 끝점 주소는
# compose 의 LM_BASE_URL 이 정한다. 나중에 외부 모델로 바꾸는 것은 Hermes
# 대시보드의 제공자 선택으로 한다.
model:
  provider: lmstudio
  name: qwen/qwen3.6-35b-a3b
