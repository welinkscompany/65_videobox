# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
*.tsbuildinfo

# OS / editor
.DS_Store
Thumbs.db

# Local env
.env
.env.*
!.env.container.example

# Build / cache
dist/
build/
.pytest_cache/
.mypy_cache/
.coverage
.ruff_cache/
.vite/
.superpowers/
.worktrees/

# App runtime artifacts
artifacts/
tmp/
.tmp/
cache/

# Playwright is invoked from the repository root by the web package script.
/test-results/
# Browser tooling drops console logs here; owner-ready reads an untracked file
# as an unexplained working-tree change and blocks on it.
/.playwright-cli/
# 화면 점검 스크립트가 캡처와 메모를 여기에 쌓는다. 다시 만들 수 있는 것이라
# 추적하지 않는다(§5 기준). 무시하지 않으면 `git add -A` 한 번에 딸려 들어가고,
# 2026-09-01에 실제로 그렇게 들어가서 커밋을 되돌려야 했다.
/output/
