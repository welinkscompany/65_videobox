# VideoBox — 통합 우선순위 계획 (2026-08-10)

owner 승인: 계획서를 하나로 모으고 우선순위를 다시 매긴다.

이 문서는 **현재 진행선의 SSOT**다. 앞선 계획서들을 폐기하지 않고, 그중 아직 살아
있는 항목만 여기로 끌어와 순서를 다시 정한다. 상세 Step이 필요하면 원본 계획서를
연다 — 여기서 그것을 복사하지 않는다.

| 원본 계획서 | 이 문서와의 관계 |
|---|---|
| `2026-08-09-videobox-diagnosability-and-structure.md` | Task 1~4 완료. Task 5만 살아 있다 |
| `2026-08-05-videobox-owner-usable-recovery.md` | Task 18·25·26·34가 살아 있다. 최종 인수 기준은 여전히 유효하다 |
| 그 이전 계획서 전부 | 닫혔다. 참조용으로만 둔다 |

## 왜 다시 매겼나

계획서가 여러 개로 갈라지면서 **제목이 실제 상태와 어긋나는 일이 반복됐다.** Task 27·31은
모든 Step이 끝났는데 제목만 `미착수`로 남아 있었고, Task 33도 `미착수 · 최우선`인 채로
이미 닫혀 있었다. 진단 계획서의 Task 5는 반대로, 적어 둔 전제(`갈래가 파일 안에 뭉쳐
있다`)가 측정으로 뒤집혔다.

그리고 2026-08-10에 **더 큰 것이 나왔다.** 아래 §측정값의 마지막 두 줄이다.

## 측정값 (2026-08-10, 전부 이 날 다시 잼)

| | 수 |
|---|---|
| 백엔드 회귀 | 3,273 통과 / **52 건너뜀** |
| 파이썬 파일 | 158 |
| 그중 기록(로깅)을 쓰는 파일 | **8** |
| 실패 이유를 버리는 `except Exception` | **349** |
| 그중 기록을 남기는 곳 | **3** |
| `api.ts` 메서드 | 147 |
| 화면 코드에 이름조차 없는 것 | **29** |
| `local_project_store.py` | 11,495줄 (파이썬 전체의 22%) |
| **운영이 쓰는 저장소** | **Postgres** |
| **그 저장소를 검증하는 테스트** | **47개 중 42개가 건너뛰어짐** |

마지막 두 줄이 이 계획서의 순서를 바꿨다. 자세한 것은 P0에 있다.

## 실행 순서 — 2026-08-10 개정 (아래 절 번호보다 이것이 우선한다)

조사 세 건이 끝난 뒤 순서를 바꿨다. **아래 P2~P4 절 번호는 그대로 두고, 실행은 이 순서를
따른다.** 번호를 다시 매기면 앞선 커밋·인계 문서의 참조가 전부 어긋난다.

| 실행 | 항목 | 절 |
|---|---|---|
| 1 | Postgres 일원화 마무리 | P0-3 |
| 2 | **되돌릴 수 없는 손상 3건** | 아래 신설 |
| 3 | 화면이 못 쓰는 기능 7개 | P3-1 |
| 4 | 나머지 기록 넓히기 (약 92곳) | P2-1 |
| 5 | 파일 분할 (믹스인) | P4-1 |

**왜 기록 넓히기를 4번으로 내렸나.** 앞서 "값어치가 가장 크다"고 적었고, 그 판단을
바꾼다. 기록은 **원인을 보이게** 하는 일이다. 그런데 조사가 찾아낸 셋 중 둘은 **보이게
해도 이미 손상된 뒤**다 — 영상 메타데이터가 날아가고 렌더된 mp4가 지워진 다음에 로그를
읽어야 되돌릴 것이 없다. **아는 손상을 먼저 막고, 그다음 모르는 것을 보이게 한다.**

화면이 못 쓰는 기능 7개를 그 위에 둔 것은, 기록이 **내가** 원인을 찾는 도구인 반면
그 7개는 **owner가 일을 하는 수단**이기 때문이다. 특히 "이 영상 꼭 써 / 이건 빼"는
촬영본이 쌓일수록 유일한 통제 수단인데 지금 저장 자체가 안 된다.

## 실행 2 — 되돌릴 수 없는 손상 3건 — [ ] 미착수 (2026-08-10 신설, 최우선)

이 셋은 기록만 더할 항목이 아니다. **동작을 바꿔야 한다.** 그래서 P2와 분리한다.

### 2-1. b-roll 등록 시 영상 정보가 영구히 날아간다 — [x] 완료 2026-08-10 (커밋 `5ca9d31be`)

실패 이유를 자산 메타데이터에 남기고, 유지보수 루프가 **나중에 다시 잰다**(한 번에 4개).

**새 표시 칸을 만들지 않고 저장된 상태에서 유도하는 방식**을 골랐다 —
`list_footage_needing_analysis`·`assets_needing_reanalysis`와 같은 형태다. 그 덕에
**이 수정 이전에 이미 망가진 자산까지 저절로 복구된다.** 등록 시점에 표시를 쓰는
방식이었으면 앞으로 들어오는 것만 고쳤을 것이다.

**함께 바꾼 것:** 가로·세로 크기를 못 읽은 경우도 조용히 넘어가고 있었는데, 그것 역시
같은 피해(세로/가로 필터에서 사라짐)를 내므로 실패로 취급한다.

**감수한 것:** 정말로 깨진 파일은 매 패스마다 경고 한 줄을 남긴다. 첫 실패에 영구
제외하면 ffmpeg 부재 같은 **고칠 수 있는 전역 원인**이 해결돼도 아무것도 복구되지
않기 때문에, 재시도 쪽을 골랐다. 이유가 그대로면 메타데이터를 다시 쓰지 않으므로
놀고 있는 스택이 DB를 두드리지 않는다.

### (원본) 2-1 문제 설명

`packages/core-engine/src/videobox_core_engine/local_pipeline.py:527` —
`_try_record_media_facts`가 ffprobe 실패를 삼키고 `return`한다. 자산은 `duration_sec`,
`width`, `height`, `orientation`, `has_audio` **없이 확정 등록**된다.
호출자 `register_broll_asset`(`:510`)은 정상 성공으로 끝난다.

**되돌릴 경로가 없다.** 등록 시점 1회 호출뿐이고 재수집 경로가 없다. 편집기에서 그 자산은
"길이 정보 없음"으로 뜨고(`editorAssetProjection.ts:63`) 세로/가로 필터에서 빠진다(`:29`).

**하는 일.** 실패를 기록하고, **나중에 다시 채울 수 있게** 한다. 라이브러리 색인이 쓰는
"아직 분석 안 된 것 다시 걸기"와 같은 방식이 이미 저장소에 있다.

### 2-2. 미리보기 발행 중 예외가 렌더된 mp4를 지운다

`packages/storage-abstractions/src/videobox_storage/local_project_store.py:919` —
소스 펜스 평가가 예외를 내면 `source_is_current = False`로 떨어지고, `:922`에서 파일을
`unlink`하고 `obsolete`로 기록한다.

**펜스가 "소스가 바뀌었다"고 판정한 것과 "펜스 자체가 터진 것"을 구분하지 않는다.**
후자는 파일을 지울 이유가 없다.

**하는 일.** 예외와 "실제로 낡음"을 가른다. 예외면 지우지 않고 이유를 남긴다.

### 2-3. 백그라운드 작업 전체가 `except: pass` 하나에 가려져 있다 — [x] 완료 2026-08-10 (커밋 `8c1ef1000`)

작업자 루프와 기동 복구 세 곳이 이유를 남긴다. **동작은 그대로다** — 실패해도 루프는
계속 돈다. 같은 테스트가 "이유가 로그에 남는가"와 "루프가 계속 도는가"를 함께 잠근다.

색인 리포트를 버리던 `main.py:288`, `:303`도 함께 고쳤다. 이제 색인 못 한 촬영본·음원의
수와 이름을 남긴다.

`services/api/src/videobox_api/main.py:516` — 분석 폴링·디스패치, 라이브러리 색인,
기동 복구, 이벤트 정리가 전부 이 안에 있다. 무엇이 계속 터져도 화면은 정상이고 로그는 없다.

손상은 아니지만 **나머지를 전부 안 보이게 만든다.** 위 둘을 고쳐도 이게 살아 있으면
다음 문제를 또 못 본다. 한 줄이다.

**같이 볼 것.** `main.py:288`, `:303`이 색인 리포트 반환값을 **변수에 받지도 않는다.**
실패를 성실히 담아 두고 버리는 구조다.

**검증.** 각각을 실제로 실패시켜, 손상이 일어나지 않고 이유가 남는 것을 본다.

**중단 조건.** 2-1과 2-2는 동작 변경이다. 회귀가 깨지고 원인이 분명하지 않으면 되돌린다.
셋을 한 커밋에 섞지 않는다.

## 고정 규칙 — 저장소는 Postgres 하나다 (owner 지시, 2026-08-10)

> "이제 앞으로 이 프로젝트에서 sqlite 는 안쓰고 모두 Postgres 이걸로 작업하도록만 해줘.
> db가 여러개면 관리가 안되잖아."

**적용 범위를 층으로 나눈다. 층마다 뜻과 비용이 다르다.**

| 층 | 지금 | 이 규칙이 요구하는 것 |
|---|---|---|
| 실행 중인 프로그램 | Postgres. 단 주소가 없으면 **조용히 SQLite로 떨어짐** | **폴백 제거.** 주소 없으면 뜨지 않는다 → P0-3 |
| 디스크에 남은 파일 | 런타임 폴더에 안 쓰는 SQLite 2벌 | 정리 → P0-3 |
| 테스트 | 73개 파일이 SQLite 임시 파일 사용 | **바꾸지 않는다** (아래 근거) |
| 코드 구조 | `PostgresProjectStore`가 `LocalProjectStore`를 **상속** | **바꾸지 않는다** (아래 근거) |

**테스트와 코드 구조를 바꾸지 않는 근거.** owner가 막으려는 위험은 "프로그램이 어느 DB를
보는지 모르는 것"이고, 그것은 위 두 층에서만 발생한다. 테스트가 쓰는 SQLite는 `tmp_path`에
만들었다 지우는 것이라 관리 대상 DB가 아니다. 그리고 **`LocalProjectStore`는 SQLite 구현이
아니라 저장소의 기반 클래스다** — 11,495줄의 로직이 전부 거기 있고 `PostgresProjectStore`는
연결과 메서드 6개만 갈아끼운다. 코드에서 SQLite를 들어내는 것은 저장소 전체 재작성이며,
owner가 겪는 문제를 하나도 해결하지 않는다.

**테스트가 SQLite만 본다는 위험은 별도로 이미 막았다** — `run-postgres-store-tests.ps1`.
더 나아가려면 73개 파일을 바꾸는 것이 아니라 **전체 회귀를 Postgres로도 돌릴 수 있게**
하는 쪽이 싸고 효과가 같다.

## P0 — 검증이 실제로 도는 것을 보게 하기

### P0-1. Postgres 경로를 회귀에 되살리기 — [ ] 미착수

**문제.** 저장소는 실행할 때 갈린다(`services/api/src/videobox_api/main.py:675`).
`VIDEOBOX_DATABASE_URL`이 있으면 `PostgresProjectStore`, 없으면 `LocalProjectStore`(SQLite).
컨테이너는 앞쪽이다. 그런데 테스트는 73개 파일이 SQLite를 쓰고, Postgres 전용 47개 중
**42개가 `VIDEOBOX_TEST_POSTGRES_URL`이 없으면 통째로 건너뛴다**
(`tests/test_postgres_project_store.py:1019` fixture). 그 변수는 지금 비어 있다.

즉 **회귀 3,273개가 전부 초록불이어도 운영에서 도는 저장소는 거의 검증되지 않는다.**
`CLAUDE.md` §4가 "가장 비싸게 배운 것"으로 적어 둔 패턴과 같은 종류다 — 테스트가 통과하는
동안 실제 경로는 한 번도 돌지 않는 것.

**하는 일.** Postgres를 띄우고 42개를 실제로 돌린다. 깨진 것이 있으면 그 자체가 결과다.
그다음 이 42개가 **다시 조용히 건너뛰어지지 않게** 한다 — 건너뛴 수가 눈에 띄게 하거나,
개발 스크립트가 Postgres를 띄워 함께 돌리게 한다.

**검증.** 42개가 실제로 돌아 통과/실패가 찍히는 것. 건너뛴 수가 0이 되는 것.

**중단 조건.** 깨진 것이 많으면 고치기 전에 먼저 목록을 보고한다. 한꺼번에 고치지 않는다.

### P0-2. 저장소가 조용히 SQLite로 떨어지는 것 드러내기 — [ ] 미착수

**문제.** `VIDEOBOX_DATABASE_URL` 한 줄이 빠지면 프로그램은 **멀쩡히 뜨고** 빈 SQLite를
본다. 경고도 기록도 없다. owner 눈에는 "프로젝트가 전부 사라짐"으로 보인다. 실제로
이렇게 생긴 SQLite 저장소가 런타임 폴더에 두 벌 남아 있었다.

**하는 일.** 시작할 때 어느 저장소를 쓰는지 한 줄 남긴다. 컨테이너 모드인데 주소가
없으면 조용히 떨어지지 않게 한다.

**검증.** 주소를 빼고 띄워서 로그에 이유가 남는 것을 본다.

### P0-3. SQLite 폴백 제거 — [x] 완료 2026-08-10 (커밋 `655584211`)

컨테이너 모드에서 주소가 없으면 **뜨지 않는다.** 판별은 `VIDEOBOX_SNAPSHOT_ROOT`
유무이므로 손으로 돌리는 개발 실행은 그대로 파일 저장소를 쓴다.

**가드를 스냅샷 검증보다 앞에 뒀다.** 뒤에 두면 스냅샷 오류가 이 오류를 가려서
정작 빠진 변수 이름이 로그에 안 뜬다 — RED 실행에서 실제로 그렇게 나왔다.

**런타임 폴더의 SQLite 2벌 정리는 보류한다 — 단순한 파일이 아니었다.**
`b-roll-smoke-test`와 `progress-bar-live-test`는 프로젝트 폴더 전체이고, 앞의 것은
**자산만 172MB**다(SQLite 파일은 528KB). 그런데 컨테이너는 DB만 Postgres로 쓰고
**파일은 같은 `runtime/projects` 아래**에 둔다(`VIDEOBOX_DATA_ROOT`). 즉 이 폴더가
Postgres 쪽 프로젝트의 파일일 가능성을 배제할 수 없다.

**Postgres를 띄워 프로젝트 목록을 확인한 뒤에 판단한다.** 확인 전에는 지우지 않는다.
`CLAUDE.md` §5의 "경로로 참조되는 곳을 먼저 찾으라"가 여기에도 적용된다 —
오늘 `task5-korean-600.wav`에서 같은 판단이 검증 두 개를 살렸다.

**하는 일.** 컨테이너 모드에서 `VIDEOBOX_DATABASE_URL`이 없으면 **뜨지 않게** 한다.
조용히 다른 저장소를 여는 경로를 없앤다. 손으로 돌리는 개발 실행은 여전히 파일 저장소를
쓸 수 있어야 하므로, 판별 기준은 "컨테이너 모드인가"(`VIDEOBOX_SNAPSHOT_ROOT` 유무)다.

런타임 폴더에 남은 SQLite 2벌(`b-roll-smoke-test`, `progress-bar-live-test`)은
컨테이너가 쓰지 않는다. 참조를 확인한 뒤 정리한다.

**검증.** 주소를 빼고 컨테이너 모드로 띄워 기동이 실패하고 이유가 로그에 남는 것을 본다.

## P1 — 위험 없이 지금 바로 되는 것

### P1-1. artifacts 정리 — [x] 완료 2026-08-10

3.6GB → 110MB. 지운 것은 `--work-root` 산출물과 지난 실행 결과
(`owner-sample-edit-20260803-r4`, `lfqa`, `long-form-capcut-qa`, `task5-smoke`,
`owner-dogfood-20260731`).

**남긴 것: `task5-korean-600.wav` (110MB).** 이건 산출물이 아니라 **입력**이다 —
`scripts/dev-fast-path.ps1:288`이 `--narration`으로 읽는다. 용량만 보고 지웠으면
검증 두 개가 깨졌다.

### P1-2. postgres 컨테이너 로그 상한 — [ ] 미착수

**문제.** 다른 네 컨테이너는 `max-size: 10m`, `max-file: 3`으로 묶여 있는데
`compose.yaml`의 `videobox-postgres`(31행)에만 `logging` 블록이 없다. 상한이 없으면
디스크를 조용히 채운다.

**하는 일.** 같은 블록을 추가한다. **검증.** `docker inspect`로 적용 확인.

## P2 — 기록 넓히기 (값어치 가장 큼)

### P2-1. 사용자 경로 밖의 침묵 걷어내기 — [ ] 미착수

**문제.** 실패 이유를 버리는 곳이 349곳인데 기록하는 곳은 3곳이다. 진단 계획서 Task 1이
owner가 밟는 다섯 곳을 고쳤지만 **나머지 346곳은 그대로다.** 이번 세션에 찾은 문제들
(굳은 분석, 폴러가 헛도는 것)도 전부 로그가 아니라 소스를 읽어서 찾았다. owner는 그렇게
할 수 없다.

**조사 완료 (2026-08-10).** 349곳 중 **실제로 삼키는 것은 95곳**이다. 나머지는
`rollback(); raise`거나 라우터의 `raise _http_error(...)`라 안전하다. 즉 문제 규모는
알려진 것의 1/4이고, 그만큼 다룰 만하다.

**가장 급한 3건 — 이 셋은 기록만 더할 것이 아니라 동작을 봐야 한다.**

1. `services/api/src/videobox_api/main.py:516` — **백그라운드 작업 전체**(분석 폴링,
   라이브러리 색인, 기동 복구, 이벤트 정리)가 `except Exception: pass` 하나에 감싸여 있다.
   무엇이 계속 터져도 화면은 정상이고 로그는 없다.
2. `packages/core-engine/src/videobox_core_engine/local_pipeline.py:527` — b-roll 등록 중
   ffprobe가 실패하면 자산이 길이·해상도·오디오 유무 **없이 DB에 확정**된다.
   **다시 채우는 경로가 없다.** 편집기에서 그 자산은 "길이 정보 없음"이 되고 세로/가로
   필터에서 아예 빠진다.
3. `packages/storage-abstractions/src/videobox_storage/local_project_store.py:919` —
   미리보기 발행 중 펜스 평가가 실패하면 `source_is_current = False`로 떨어져
   **렌더된 mp4를 지우고**(`:922`) `obsolete`로 기록한다. 파괴적이고 되돌릴 수 없다.

**클러스터 — 실패를 기록하는 척하는 것.** `library_footage_indexer.py:120`과
`library_audio_indexer.py:128`은 실패를 `report.failed`에 성실히 담는데, 호출하는 쪽
(`main.py:288`, `main.py:303`)이 **반환값을 변수에 받지도 않는다.** 촬영본·음악이 색인되지
않아도 아무 데도 안 남는다. 음악 추천이 늘 "분위기만" 모드인 원인일 수 있다.

**따를 모범이 이미 저장소 안에 있다.** `recommenders.py:141`, `review_guidance.py:204`,
`director_proposal_service.py:234`는 예외를 먹되 `provider_trace`에
`final_provider="heuristic_fallback"`과 `additional_reason`을 남긴다. 위 항목들을 이 형태로
맞춘다.

**하는 일.** 위 3건을 먼저, 그다음 클러스터. 나머지는 이 형태를 따라 넓힌다.

**검증.** 각 지점을 실제로 실패시켜 로그에 이유가 남는 것을 본다. 컨테이너 로그에서 확인.

**중단 조건.** 기록만 더하는 항목에서 동작이 바뀌면 되돌린다. 위 2·3번은 동작 변경이
필요할 수 있으므로 **별도 판단**하고 한꺼번에 섞지 않는다.

## P3 — 만들어 놓고 못 쓰는 기능

### P3-1. 화면이 안 부르는 29개 — [ ] 미착수

**문제.** `api.ts` 147개 중 29개가 화면 코드에 이름조차 없다. 2026-08-09에 31개였고
프리셋 5개를 붙였는데 지금 29개다 — **붙이는 속도만큼 새로 만들고 있다.**

직접 확인한 것: `getExport`, `buildTimeline`, `createHermesRun`, `getProject`는 화면
코드에 호출이 0건이다. 나머지는 목록으로만 확인했다.

**분류 완료 (2026-08-10). WIRE 7 / DELETE 13 / KEEP 9.**
**WIRE 7개 전부 화면에 붙였다 — [x] 완료 2026-08-10** (프런트 845 통과 / 빌드 통과).
아래 목록은 무엇이 왜 막혀 있었는지의 기록으로 남긴다. 붙이면서 드러난 함정 셋:

- **취향 저장은 네 갈래를 통째로 보내야 한다.** 서버가 키별로 병합하되 그 키의
  리스트는 통째로 교체한다. 방금 누른 값만 보내면 **앞서 제외한 자산이 조용히
  풀린다.** 마지막 서버 상태에서 네 리스트를 다 계산해 보낸다.
- **`setState` 갱신 함수 안에서 payload를 만들면 첫 클릭이 빈 리스트로 나간다.**
  갱신 함수는 다음 렌더에서 돌기 때문이다. ref에서 유도하도록 고쳤다.
- **pin과 exclude를 동시에 걸 수 있었다.** 그러면 백엔드가 "항상 쓰라"고 한 자산을
  후보에서 뺀다. 서로 배타로 만들었다.

`onPreviewCandidate: () => undefined`는 빈 함수가 아니라 **죽은 줄**이었다 —
`EditorWorkbench.tsx:274`가 이미 동작하는 구현으로 덮어쓰고 있었다. 지웠다.

**아직 브라우저에서 밟지 않았다.** `CLAUDE.md` §4 기준으로는 이것이 진짜 완료 조건이다.



**WIRE — owner가 못 쓰는 실제 기능 (아쉬움 순)**

1. `getDirectorPreferences` · `updateDirectorPreferences` — "이 자산 꼭 써 / 이건 빼"를
   저장하는 손잡이. 값이 유진의 자산 순위에 실제로 들어가는데(`media_ranking.py:32,44`),
   **화면이 저장을 안 부르니 취향은 영원히 빈 값**이고 해당 점수는 항상 0이다.
   유진 채팅으로도 못 바꾼다(`director_commands.py:50`은 참조 코드만 다룬다).
   촬영본이 쌓일수록 유일한 통제 수단이다.
2. `directorCandidatePreviewUrl` — 후보를 눌러 볼 수가 없다. **두 겹으로 막혀 있다:**
   유진 후보 어댑터가 `preview_uri`를 `None`으로 넣고(`yujin_creator_proposal_adapter.py:796`)
   버튼은 그 값이 있을 때만 뜨며(`RightDock.tsx:220`), 떠도 핸들러가 빈 함수다
   (`EditorWorkbenchRoute.tsx:1481`). "썸네일 보고 고른다"가 막혀 있다.
3. `refreshDirectorProposal` — 제안이 stale이 되면 화면이 `blocked`으로 바뀌고
   (`EditorWorkbenchRoute.tsx:1381`), 새 제안 시작은 기존 제안이 남아 있어 비활성이다
   (`:1482`). **한 번 stale이면 그 화면에서 유진으로 돌아갈 길이 없다.** 이게 복구 경로다.
4. `getMediaLibraryInstallState` — 라이브러리가 비면 제목만 뜨고 이유를 알 수 없다
   (`MediaLibraryBrowser.tsx:33,79`). 기능이 아니라 진단이다.
5. `listRecentEditorPresetIds` · `listRecentMediaLibraryAssetIds` — **쓰기만 하고 읽지
   않는다.** 최근 사용을 꼬박꼬박 기록하는데(`CaptionPresetPicker.tsx:96`,
   `media_library.py:192`) 보여 주는 화면이 없다. 둘은 같은 성격이라 함께 붙인다.

**DELETE 13 — 근거는 조사 보고에 있다.** 대체 경로가 이미 화면에서 도는 것들
(`applyDirectorProposal`은 `batchApplyDirectorProposal`이, `buildTimeline`·
`createEditingSession`은 `createAtomicDraftBundle`이 대신한다). `listProjectRecentMediaLibraryAssetIds`는
**양쪽이 다 죽었다** — 기록 함수 `mark_project_media_library_recent`를 부르는 곳이 없어
목록이 항상 빈 배열이다.

**KEEP 9 — 지우면 안 된다.** 유진 Hermes 스트리밍 4개는 owner 결정으로 로컬 경로를 쓰는
것이다(`docs/decisions/2026-08-05-local-first-assistant-decision.ko.md`). 레거시 4개는
**테스트가 "부르지 않음"을 잠가 뒀다**(`task22-parity-owners.test.ts:200`,
`AppRouter.test.tsx:253,405`) — 지우면 그 가드가 사라진다.

**`approveReviewRecommendation`은 내 앞선 추측이 틀렸다.** `VIDEOBOX_AUTO_APPROVE_SEGMENT_REVIEW=1`
때문이 아니다. 그 플래그는 검토 플래그만 무력화하고 보류 추천은 여전히 승인을 막는다
(`_pipeline_private_helpers.py:265`). 실제 이유는 검토 화면이 **의도적으로 읽기 전용**이고
(`TimelineReviewPage.test.tsx:201`이 버튼 없음을 못박음), 현행 초안 경로가 보류 추천을
아예 만들지 않기 때문이다(`local_project_store.py:2664`).
**남는 위험:** 어떤 경로로든 보류 추천이 생기면 화면에서 풀 방법이 없어 내보내기가 통째로
막힌다. 지금은 "발생 경로가 없다"가 유일한 방어다.

**검증.** 붙인 것은 브라우저에서 owner 경로로 밟는다.

**같이 정할 것.** 새 백엔드를 만들 때 부르는 화면을 같이 만드는 규칙이 없으면 이 목록은
계속 는다. 규칙을 세울지 owner와 정한다.

## P4 — 구조 정리 (화면이 안 바뀜)

### P4-1. `local_project_store.py` 분할 — [ ] 미착수

11,495줄, 파이썬 전체의 22%. **owner 화면에서 달라지는 것은 없다.**

**조사 완료 (2026-08-10). 방식이 정해졌다 — 믹스인이어야 한다.**

별도 모듈로 떼면 깨진다. **`_connection`(기반)이 `_ensure_hermes_capability_lifecycle_schema`
(hermes)를 호출한다** — 기반이 갈래를 역참조하므로 모듈로 분리하면 실제 순환 import가 된다.
게다가 인스턴스에 몽키패치를 거는 테스트 4개가 조용히 무력화되고,
`PostgresProjectStore._connection` 오버라이드가 위임 객체에 전달되지 않아 **Postgres 경로가
SQLite 커넥션을 쓸 수 있다.**

믹스인이면 결합을 끊지 않고 **파일만** 나눈다. 이 파일의 결합은 전부 `self.` 조회라
MRO가 그대로 흡수한다. 공개 표면(`dir()` 기반 스냅샷)도 유지되고,
**`postgres_project_store.py`는 한 줄도 고치지 않는다.**

```
local_project_store.py        ← class LocalProjectStore(HermesCapabilityMixin, ...)
_store_hermes_capability.py   ← class HermesCapabilityMixin  (18개 + 상수 3개)
```

**hermes가 첫 순서인 것이 측정으로 확인됐다.** 외부 의존이 `_connection`, `_now_iso`,
`_fetchone` **3개뿐**이다(media 8개, yujin 7개). 조각 A(`1166–1866`, 17개)는 중간에 다른
갈래가 하나도 안 끼어 있어 통째로 잘라내면 된다.

**지뢰 하나.** `_validate_hermes_expected_scope:1844`가 `LocalProjectStore.`를 문자 그대로
참조한다. 이 한 줄은 클래스명을 바꾸거나 그 메서드만 기반에 남겨야 한다. 계획에서 빠뜨리면
순환 import로 막힌다.

**`director_hermes` 계열 11개는 이번에 손대지 않는다.** 도메인상 Hermes지만 director·yujin과
얽힌 210줄짜리 중간 지대를 함께 건드려야 한다. **"hermes = capability 원장만"**으로 못박아
다음 세션이 두 번 옮기지 않게 한다.

**계획서의 이전 숫자 두 개가 틀렸다.** director 44 → 실제 43(`_asset_directory`가
"director"에 걸린 문자열 오탐), review 25 → 실제 4 수준(`preview`가 "review"에 걸림).
director·review를 계획할 때 그 숫자를 그대로 믿으면 안 된다.

`tests/test_store_split_preserves_the_surface.py`가 공개 메서드 **318개**를 잠가 뒀다
(계획서가 적은 317은 오기이며, 현재 코드와 차이 0으로 동기화돼 있다).

**선행 조건 (새로 추가).** P0-1을 먼저 끝낸다. `PostgresProjectStore`가 이 파일을
상속하므로, **Postgres 경로가 검증되지 않은 상태에서 분할하면 깨져도 모른다.**

**중단 조건.** 한 갈래에서 회귀가 깨지고 원인이 분명하지 않으면 되돌리고 멈춘다.
절반만 된 분할은 안 한 것보다 나쁘다.

### P4-2. 저장만 하고 읽지 않는 데이터 정리 — [ ] 미착수 (원본: 복구 계획서 Task 34)

부채 정리. 우선순위 낮음.

## owner 몫 — 내가 할 수 없는 것

| 항목 | 원본 | 왜 owner 몫인가 |
|---|---|---|
| **전체 흐름 실제 사용** | 복구 계획서 최종 인수 기준 | 통과 기록이 없다. 이것이 마지막 빈 칸이다 |
| Task 25 내 목소리 TTS | 복구 계획서 | owner가 2026-08-05에 보류 결정 |
| Task 26 마이크 실측 | 복구 계획서 | owner 참관 필요 |
| Task 18 촬영본 실제 이동 | 복구 계획서 | 데이터를 옮기는 결정 |
| Hermes 실제 provider 로그인 | `CLAUDE.md` §6 | 승인 필요 |
| `main` 브랜치 처리 결정 | — | 작업선이 main보다 426 커밋 앞서 있다 |

### 테스트 전에 알아야 할 것

- **창 너비 1600 이상.** 아니면 편집기 오른쪽 독이 하나만 열려 기능이 없는 것처럼 보인다.
- **LM Studio를 켜 둘 것.** 안 켜면 분석이 전부 `blocked`로 떨어진다.
- **새 영상 반입 목록은 비어 보인다.** 컨테이너가 보는 `drive-sync/새 영상`이 비어 있다.
  촬영본 726MB는 컨테이너가 안 보는 폴더에 있다. 이미 들어온 것은 라이브러리에 1.2GB 있어
  편집 테스트 자체는 된다.

## 공통 규칙

- 각 항목은 `RED → GREEN → 좁은 검증 → 넓은 검증`
- **테스트 통과는 완료가 아니다.** owner 경로를 실제 런타임에서 밟는다
- 각 항목이 논리적으로 닫히면 커밋·푸시
- 앞 항목이 막히면 뒤로 넘어가지 말고 보고한다
- **회귀가 도는 중에 소스를 고치지 않는다.** 2026-08-10에 두 번 겪었다
- 컨테이너를 쓰는 작업은 owner가 화면을 쓰는 중에 돌리지 않는다 — 같은 스택이다
