# VideoBox — 진단 가능성과 구조 정리 (2026-08-09)

owner 승인: 아래 다섯을 전부 진행한다.

## 왜 이 순서인가

이번 세션에 찾은 문제들 — 15시간 굳은 작업, 폰트 오타, 초당 180건 DB 폴링, 204를
실패로 읽던 버그 — 이 **하나도 로그에 없었다.** 전부 소스를 읽거나 직접 눌러 봐서
찾았다. owner는 그렇게 할 수 없다.

그래서 로깅이 먼저다. 그다음이 "조용히 나빠지는 것"을 말하게 하는 일이고, 구조
정리는 owner 화면에서 달라지는 게 없으므로 마지막이다.

측정값 (2026-08-09):

| | 수 |
|---|---|
| 파이썬 파일 | 158 |
| 로깅을 쓰는 파일 | 3 |
| 이유를 버리는 `except Exception` | 347 |
| 그중 기록을 남기는 것 | 1 |
| `api.ts` 메서드 | 147 |
| 화면 코드에 이름조차 없는 것 | 31 |
| `local_project_store.py` | 11,463줄 / 메서드 320개 |

## Task 1 — 로깅 (사용자 경로부터) — [x] 완료 2026-08-10

**문제.** 실패가 어디에도 남지 않는다. 347곳이 이유를 버리고 1곳만 기록한다.

**범위.** 전부가 아니라 **owner가 밟는 경로**부터. 운영 점검이 심각도순으로 매긴 것:

1. `yujin_memory_service.py:170` — 기억 조회 전체를 삼킨다. 게이트웨이 장애가
   "기억이 원래 없음"과 구분되지 않는다
2. `routers/media_inbox.py:75` — 드롭 폴더 영상의 분석 예약 실패. 매일 쓰는 경로다
3. `hermes_run_service.py:1275, 1022` — 대화·초안 저장 실패
4. `agent_gateway_client.py:199` — 게이트웨이 응답 파싱 실패

**하는 일.** 공용 로거 설정을 두고, 위 네 곳이 삼킬 때 **왜 삼켰는지**를 남긴다.
동작은 바꾸지 않는다 — fail-open은 그대로 두고 기록만 더한다.

**검증.** 각 지점을 실제로 실패시켜 로그가 남는 것을 본다. 컨테이너 로그에서 확인.

## Task 2 — 조용히 나빠지는 것을 말하게 하기

**문제.** 임베딩 조회가 실패하면 `except Exception`으로 단어 매칭에 떨어지는데
화면에 아무 표시가 없다. 추천이 갑자기 나빠져도 owner는 원인을 모른다.

**하는 일.** 추천 응답이 "의미로 찾았는지 / 단어로만 찾았는지"를 싣고, 화면이
그것을 말한다. 유진의 이유 문장에도 반영한다.

**검증.** 임베딩 공급자를 끊고 추천을 돌려서 화면이 그 사실을 말하는지 브라우저에서 본다.

## Task 3 — 화면이 안 부르는 API 31개

**문제.** 147개 중 31개가 화면 코드에 이름조차 없다. 만들어 놓고 못 쓰는 기능이다.

**하는 일.** 31개를 훑어 세 갈래로 가른다 — **화면에 붙일 것**, **지울 것**,
**의도적으로 남길 것**(외부 도구·테스트용). 판단 근거를 문서에 남긴다.
붙일 것 중 값어치가 큰 것부터 실제로 연결한다.

**검증.** 붙인 것은 브라우저에서 owner 경로로 밟는다. 지운 것은 타입 검사와 전체 테스트.

## Task 4 — 옛 b-roll 태그 재분석

**문제.** 새 분석은 우리말로 나오지만 이미 저장된 프로젝트 분석은 영어다.
편집기에서 같은 영상이 두 언어로 보인다.

**하는 일.** 분석 결과에 프롬프트 버전을 달고, 낡은 것을 다시 분석 대기로 돌린다.
라이브러리 색인이 쓰는 것과 같은 방식이다.

**검증.** 실제 프로젝트에서 태그가 우리말로 바뀌는 것을 확인.

## Task 5 — `local_project_store.py` 분할

**문제.** 11,463줄, 메서드 320개. 파이썬 전체의 22%가 한 파일이다. 이번 세션에
직접 헤맸고, 존재하지 않는 헬퍼를 있다고 착각해 코드를 잘못 짰다.

**위험.** `PostgresProjectStore`가 상속한다. 쪼개면 상속 순서가 바뀐다.
owner 화면에서 달라지는 것은 **없다.**

**하는 일.** 이름으로 드러난 경계를 따라 **한 갈래씩** mixin으로 뗀다.
director(44) → media(33) → yujin(27) → review(22) → hermes(18) 순.
한 갈래마다 전체 회귀를 돌린다. 동작은 한 줄도 바꾸지 않는다.

**중단 조건.** 한 갈래에서 회귀가 깨지고 원인이 분명하지 않으면 되돌리고 멈춘다.

## 부수 항목

- postgres 컨테이너 로그 상한 (다른 컨테이너는 10MB×3, postgres만 무제한)
- `media_analysis_cache`에 실제로 얼마나 쌓여 있는지 확인 — 첫 정리가 큰 삭제일 수 있다

## 공통 규칙

- 각 Task는 RED → GREEN → 좁은 검증 → 넓은 검증
- **테스트 통과는 완료가 아니다.** owner 경로를 실제 런타임에서 밟는다
- 각 Task가 논리적으로 닫히면 커밋·푸시
- 앞 Task가 막히면 뒤 Task로 넘어가지 말고 보고한다
