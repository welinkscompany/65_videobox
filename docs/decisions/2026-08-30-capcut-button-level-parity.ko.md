# 캡컷 벤치마킹 — 버튼 단위까지 확장 (구성·배치·크기) — 승인 기록

- 결정 상태: **approved**
- 결정 시각: 2026-08-30
- 승인자: owner (루이스 대표님)
- 확장하는 것: `2026-08-29-capcut-full-structure-and-dark-theme.ko.md`의
  "메뉴·배치 구조를 캡컷과 완전히 동일하게" 범위를 화면 구조 수준에서
  **컴포넌트 단위**로 좁혀 확정한다. 그 문서를 대체하지 않는다 — 그 문서가
  정한 것(5단계 계획, 다크 팔레트, 기능 제외 목록)은 전부 그대로 유효하다.

## owner가 말한 것 (원문)

> "그리고 캡컷 벤치마킹은 나는 버튼 하나까지도 다 벤치마킹 하라는거야. 버튼
> 구성, 버튼 배치, 크기 등등 모두다 벤치마킹해"

## 승인된 것

**VideoBox에 이미 있는 기능의 화면 요소는 캡컷과 같은 자리·같은 구성·비슷한
크기로 맞춘다.** 화면 구조(메뉴·탭·패널 배치)뿐 아니라 그 안의 버튼 단위까지
내려가서:

- 버튼 구성 — 어떤 버튼들이 한 자리에 같이 있는지, 순서는 어떤지
- 버튼 배치 — 화면의 어느 위치(좌/우/상단바/하단 고정 등)에 놓이는지
- 버튼 크기 — 아이콘 크기, 버튼 높이/폭, 여백

를 캡컷 화면을 근거로 맞춘다.

## 범위 밖 — `2026-08-29` 문서의 원칙을 그대로 유지

- **VideoBox에 없는 기능의 버튼 자리를 만들지 않는다.** 캡컷에 있지만
  VideoBox 범위 밖인 기능(멀티캠, 임의 키프레임, 전문 색보정 등,
  `implementation-plan.ko.md` §4)의 버튼은 이 문서로도 추가하지 않는다.
  이 문서는 **있는 기능을 어떻게 배치할지**를 다루지, 없는 기능을 새로
  만들라는 뜻이 아니다.
- 색상 팔레트는 `2026-08-29` 문서가 이미 확정했다 — 이 문서가 다시 열지 않는다.

## 실행 시 판단 기준 (재사용 게이트용)

- 근거는 `docs/decisions/assets/`에 저장된 캡컷 캡처 3장이다(2026-08-30
  세션에서 과거 대화 로그를 뒤져 파일로 복구, `2026-08-29` 문서에 경위
  기록):
  - `2026-08-29-capcut-home-screen.webp` — 첫 화면(배너·AI 카드·최근 프로젝트)
  - `2026-08-29-capcut-editor-screen.png` — 편집기 화면 전체 배치
  - `2026-08-29-capcut-editor-panel-resize-markup.png` — 크기조절 경계 표시
  화면별로 이 세 장 밖의 영역이 추가로 필요하면 owner에게 새 캡처를 요청한다.
  픽셀 단위로 추출한 정확한 값이 아니라 근사치로 시작하고, 구현 후 실제
  화면을 보고 조정한다 — 다크 팔레트 때와 같은 방식(`2026-08-29` 문서
  56행 참고).
- 화면(컴포넌트)마다 개별 진행 — 한 번에 전체를 다시 짜지 않는다. 어떤
  화면부터 시작할지는 owner 우선순위를 따른다.
- 기존 컴포넌트(shadcn/ui 등) 재사용을 우선한다 — 크기·배치만 캡컷에
  맞추고 컴포넌트 자체를 새로 짜지 않는다.

## 함께 감수하는 것

- 버튼 크기·배치가 바뀌면 관련 스냅샷·접근성(대비, 터치 타깃 크기) 테스트가
  재작성이 필요할 수 있다.
- 범위가 넓다 — 화면이 많아 한 세션에 다 끝내지 못할 가능성이 높다. 어떤
  화면부터 손댈지 우선순위를 owner와 다시 확인한다.

## 진행 상태

### 1단계 완료 — 편집기 상단 도구줄에서 편집 동작 분리 (커밋 `d5fd8012`)

캡컷 편집기 캡처를 실제로 대조해 보니, 편집 동작(되돌리기·다시 실행·
나누기·앞과 붙이기·빼기·다음 장면에도)은 상단 도구줄이 아니라 **타임라인
바로 위, 확대·축소와 같은 줄**에 있었다. VideoBox는 이 모두가 상단 한
줄에 섞여 있었다 — `TimelineDock`이 이미 갖고 있던 그 줄(줌 조작 자리)에
`editToolbar` 슬롯을 만들어 그대로 옮겼다. 상단은 이제 미디어·세부 정보
토글 + 내보내기만 남았다. 재빌드 후 브라우저에서 실제 위치 확인 완료.

### 판단이 필요해 남긴 것 — 상단 도구줄의 정보 구조 자체

owner에게 물어 **"캡컷처럼 콘텐츠 탭으로 재구성"**을 확인받았다. 다만
이건 1단계보다 훨씬 큰 변경이다: 캡컷 상단 줄(미디어·오디오·텍스트·
스티커·편집효과·전환·캡션·필터·조정·템플릿·AI 아바타)은 **왼쪽 패널에
늘 떠 있는 콘텐츠 카테고리 탭**이고, VideoBox의 미디어·세부 정보는
**패널을 열고 닫는 토글 버튼**이다(패널 안에 미디어·오디오·자막·전환
탭이 한 단계 더 들어가 있다). 캡컷처럼 만들려면 지금의 서랍(drawer)/
도크(dock) 열고 닫기 모델 자체를(반응형 폭 대응 포함) 상시 노출형 패널 +
최상위 탭 전환 모델로 바꿔야 한다 — 리사이즈 가능 패널·좁은 화면 서랍
전환 로직까지 얽혀 있어 1단계보다 위험이 훨씬 크다.

**owner 확인**: "결국은 다 바꿀 건데 어떤 걸 먼저 하는 게 나아?"라고
물어, 구조부터 먼저 하는 게 낫다는 답(작은 단위를 먼저 다듬으면 구조가
바뀔 때 이중 작업이 된다)과 함께 "제대로 확실하게 짚어가며 진행"을
지시받아 착수함.

### 2단계 완료 — 왼쪽 패널 콘텐츠 탭을 편집기 맨 위로 승격 (커밋 `56511c0c`)

`EditorAssetBrowser`가 2026-08-27에 이미 만들어 둔 탭(미디어·오디오·자막·
전환, `vb-editor-assets__panes`)이 패널 **안**에 있던 것을 패널 **바깥**,
편집기 맨 위로 옮겼다. 예전 "미디어" 아이콘 하나가 패널을 열고 닫기만
했다면, 이제 이 네 탭이 늘 보이고 누르면 그 내용으로 패널이 열린다 —
캡컷과 같은 동작이다. `EditorAssetBrowser`의 탭 상태를 제어 가능(controlled)
하게 바꿔서(`pane`/`onPaneChange`/`renderPaneTabs`), 패널 내용 로직은
그대로 재사용하고 탭 버튼만 `EditorWorkbench` 맨 위로 옮겼다 — 두 번
짜지 않았다.

**한 가지 조심한 것**: 데스크톱 모드에서는 같은 탭을 다시 누르면 패널이
접힌다(전체 화면 미리보기, owner 승인 2026-08-17을 여전히 쓸 수 있게).
**서랍(좁은 화면) 모드는 이 접기를 안 한다** — 예전 단추도 서랍에서는
항상 열기만 했지 닫은 적이 없어서, 접는 동작을 더했다가 시험 4건이
실패하며 그 차이를 실측으로 확인했다.

재빌드 후 브라우저에서 실제로 "미디어 / 오디오 / 자막 / 전환 / 세부 정보 /
내보내기"가 맨 위에 늘 보이고, "오디오"를 누르면 패널이 곧바로 음악
목록으로 열리는 것을 확인했다.

### 3단계 완료 — 오른쪽 패널("세부 정보")도 상시 노출로 전환

owner 확인 후("오른쪽 패널도 이어서 상시 노출로 바꿔줘") 착수. 이 시점엔
오른쪽 패널이 정말로 탭 하나(선택한 클립 속성 + 유진 AI 대화가 한 패널
안에 같이 있음)였다 — 그래서 왼쪽처럼 탭을 패널 밖으로 승격하는 작업은
필요 없었고, **이미 열려 있어도 다시 눌러 닫을 수 있던 것만** 캡컷과
다른 점이었다.

**이 문단은 2026-08-30 같은 날 뒤에 낡았다.** owner가 실제 설치본을 써 보고
"내가 구분해서 탭으로 정리하라고 했더니 하나도 안 하고 그냥 다 때려박아
넣었구나"라고 지적 — 위 문단이 "전환할 대상이 없다"고 적은 전제 자체가
틀렸다는 뜻이었다. 6단계에서 속성/유진/추천 세 탭으로 실제로 나눴다.
아래 두 문단(`openRightPane` 단추 동작)은 여전히 유효하다 — 그때 바꾼 것은
"열림/닫힘"이었고, 6단계가 바꾼 것은 "열린 다음 안에서 뭘 보여주는가"라서
서로 겹치지 않는다.

`EditorWorkbench.tsx`에 `openRightPane` 함수를 추가해 "세부 정보" 단추의
동작을 바꿨다: 안 보이면 열고(중간 폭 화면에서 왼쪽이 보이고 있었다면
`toggleDock`의 기존 `closesTheOther` 조정이 왼쪽을 대신 접는다), 이미
보이면 아무 것도 하지 않는다 — 캡컷의 정보 패널처럼 자기 단추로는 못
닫는다. 서랍(좁은 화면) 모드는 손대지 않았다: `openDrawer`를 그대로 불러
열고, 닫기는 여전히 서랍의 `닫기` 단추·Esc로만 한다.

`EditorAssetBrowser`처럼 패널 안 내용을 다시 짜지 않았다 — 단추 하나의
클릭 동작만 바꿨다. 재빌드 후 브라우저에서 실제로 확인: 데스크톱
단일-도크 폭에서 "세부 정보"를 열고 다시 눌러도 안 닫히고, 좁은 화면
서랍 모드에서는 열기·`닫기` 단추 닫기가 그대로 동작했다. 프런트엔드
전체 시험 1,371개 통과.

**감수한 것**: 왼쪽·오른쪽이 동시에 열리는 `desktop-both` 모드로 가는
폭 기준 자체는 이번에 바꾸지 않았다 — persisted `leftOpen`/`rightOpen`이
둘 다 참이어야 하는 기존 규칙 그대로다. 이번 지시는 "닫을 수 없게"였지
"기준 폭을 넓혀라"가 아니었다.

### 4단계 착수 — 첫 화면("프로젝트" 목록), 실측 중 시스템 버그 발견해 먼저 고침

owner "진행해" 지시로 다음 화면(`/projects`)의 버튼 단위 대조를 시작했다.
`docs/decisions/assets/2026-08-29-capcut-home-screen.webp`(캡컷 첫 화면)의
"+ 프로젝트 만들기" 배너는 두드러지게 큰(약 140px 높이 추정) 둥근 CTA인
반면, VideoBox의 "+ 새 프로젝트 만들기"는 실측 결과 높이 35px, 모서리
반지름 0px로 렌더링되고 있었다 — `.vb-catalog-create`의 CSS 소스는
`min-height: 5rem`(80px)·`border-radius: var(--vb-radius-md)`를 이미
지정하고 있었는데도(캡컷 대조를 이미 염두에 둔 값), 실제로는 전혀
반영되지 않고 있었다.

**원인은 CapCut 대조와 무관한 기존 CSS 결함 두 가지였다** (개발자 도구로
`getComputedStyle`·`document.styleSheets` 직접 대조해 확인, 화면 느낌이
아니라 실측):

1. `styles/product-shell.css`의 "Intranet DS normalization" 블록이
   `.vb-product-shell [data-slot=button][data-variant="default"]`에
   `border-radius: var(--radius-2xl)`를 걸고 있었는데, `--radius-2xl`은
   **정의된 적이 없는 변수**였다 -- 반지름 척도를 셋(`--vb-radius-sm/md/lg`,
   owner 결정 2026-08-27)으로 줄이면서 이 두 줄만 옛 이름 그대로 남았다.
   정의 안 된 `var()`는 선언 자체를 무효로 만들어 초기값(0)으로 떨어진다 --
   그래서 **이 화면 전체의 default·outline 버튼이 전부** 모서리 없이
   각지게 그려지고 있었다. `features/footage/footage.css`에도 같은
   패턴이 두 줄 더 있었다(그중 하나는 `footage-design-system.test.ts`가
   깨진 값 그대로를 "정답"으로 박아 두고 있었다 -- 그 시험도 함께 고쳤다).
2. 같은 블록의 `.vb-product-shell [data-slot=button] { min-height:32px; }`은
   속성 선택자를 써서 특정도가 `.vb-catalog-create`(클래스 하나)보다 높다.
   그래서 이 버튼만 크게 만들려던 `min-height: 5rem`이 이 일반 규칙에
   조용히 눌려 한 번도 반영된 적이 없었다.

**고친 것**: `--radius-2xl` 두 자리를 전부 `--vb-radius-md`로 바꾸고,
`.vb-product-shell [data-slot=button].vb-catalog-create { min-height:5rem; border-radius:var(--vb-radius-md); }`를
같은 특정도로 뒤에 추가해 이 버튼만 이긴다. 다른 버튼(일반 default·outline)의
`min-height:32px`는 그대로 뒀다 -- 그건 버그가 아니라 "Intranet DS h-8
역할 높이" 의도된 값이다(`footage-design-system.test.ts`의 "keeps ...
h-8 role height" 시험이 그 의도를 명시).

**검증 완료** (커밋 `9378c5c0`): 백엔드 전체 pytest 4193 passed·56
skipped·0 failed, 프런트엔드 전체 1,371 passed. 재빌드 후 브라우저
`getComputedStyle` 실측 — "+ 새 프로젝트 만들기"는 높이 80px·모서리
반지름 10px(`.625rem`)로 확인, 옆의 일반 버튼("격자로 보기")은 높이
35px를 그대로 유지하면서 모서리만 10px로 고쳐졌다 — 의도한 대로 이
CTA만 커지고 나머지 버튼 높이는 안 건드렸다.

### 5단계 완료 — 보기전환·보관함 단추를 아이콘으로, 더 큰 시스템 결함 하나 더 발견 (커밋 `521f3c67`)

`docs/reference/capcut-observed-2026-08-22.ko.md` §1을 먼저 확인 —
"보기전환이 있어야 한다"는 이미 정해졌지만 **아이콘이냐 텍스트냐는
그 기록이 정한 적이 없었다**(캡컷은 "오른쪽에 검색·보기전환·휴지통·
프로젝트 동기화"라고만 적혀 있고, 실제로는 좁은 아이콘 줄이다). 그래서
재승인 없이 기존 결정을 덮어쓰는 문제는 아니었다.

"격자로 보기"·"줄로 보기"·"보관함 보기"를 전체 폭 텍스트 단추에서
아이콘 단추(`LayoutGrid`·`List`·`Archive`)로 바꿨다. 접근성 이름은
그대로 유지(`title` + `sr-only`) — 기존 시험 57개가 코드 수정 없이
그대로 통과했다.

**이 과정에서 더 큰 결함을 하나 더 발견했다**: 아이콘이 축소되지
않고 원본 크기(24px)로 나왔고, 세 단추 크기도 서로 달랐다(48x40 /
40x32). 원인을 실측으로 확인 — **이 빌드는 Tailwind의 번호 척도
유틸리티(`size-4`·`size-9`뿐 아니라 `h-9`·`gap-2`·`px-4`까지) 자체를
한 번도 컴파일한 적이 없다**(컴파일된 CSS 12만 자를 전부 뒤져도 이
문자열들이 안 나온다). 에디터 툴바가 이미 같은 문제를 겪고
`.vb-editor-workbench__toolbar [data-slot="button"] svg { width:1rem; height:1rem; }`로
우회해 뒀던 것을 발견해, 같은 값으로 `.vb-catalog` 범위에 맞춰
추가했다. 정사각형 아이콘 단추 크기(`width:2rem`)도 같이 정리.

**이 문단은 낡았다 — 2026-08-30 후속 세션에서 원인을 찾아 고쳤다.**
`apps/web/src/ui-system.css`가 `@import "tailwindcss/utilities"`만
불러오고 `tailwindcss/theme.css`는 안 불러오는 게 원인이었다 — 그
파일의 `--spacing: 0.25rem` 하나가 `size-*`·`h-*`·`w-*`·`gap-*`·
`p-*`·`m-*` 전부의 계산식에 쓰여서, 이 값이 없으면 컴파일러가 그
유틸리티들을 아예 만들지 않는다(계산이 틀리는 게 아니라 클래스 자체가
없었다). owner 확인 후(대표님: "Tailwind 유틸리티 버그부터 조사해봐" →
결과 보고 후 "지금 적용하고 화면 전체를 훑어서 검증") `@theme` 블록에
`--spacing: 0.25rem;` 한 줄을 추가해 고쳤다 — 이미 있던 두 우회
(에디터 툴바·이 화면의 `svg { width:1rem }`)는 정확히 같은 값이라
충돌 없이 그대로 둬도 된다. 프런트엔드 전체 시험 1,371개 통과 후
브라우저에서 카탈로그·편집기 세 탭(속성/유진/추천)을 훑어 가로 넘침·
크기 이상 없음을 확인했다. 커밋 `b5cefd67`.

**남은 것**: 첫 화면의 퀵스타트 카드 2장은 대체로 이미 맞아 있다
(2026-08-29 결정 문서가 이미 다뤘음). 검색창 자체(입력칸 폭·자리)는
버튼이 아니라서 이번 범위 밖으로 남겨 뒀다.

### 6단계 완료 — 설치본을 직접 써 본 뒤 나온 결함 4건 (커밋 `64718624`)

owner가 설치된 Tauri 앱을 직접 써 보고 네 가지를 지적했다: 스크롤이
창 끝에 안 붙어 있다, "+ 새 프로젝트 만들기"가 편집기로 바로 안 간다,
유진 대화창을 탭으로 나누라고 했는데 안 했다, 영상 재생·스크롤 같은
네이티브 컨트롤이 다크와 안 맞는다. 넷 다 재현·확인 후 고쳤다.

**스크롤이 창 끝에 없던 것** — Tauri만의 문제가 아니라 순수 CSS 버그였다
(같은 폭의 일반 크롬 탭에서도 재현). 가운데 정렬·`max-width:1200px`인
`.vb-product-content`가 자기 `overflow-y:auto`를 갖고 있어서, 창이 그
너비보다 넓을 때 스크롤바가 콘텐츠 박스 오른쪽 끝에 붙었다 — 창 오른쪽
끝이 아니라. `overflow-y`를 전체 폭 부모 `.vb-product-main`으로 옮기고
편집기 경로만 예외(`:not(:has(.vb-editor-workbench))`)로 남겼다(편집기는
자기 안에서 따로 스크롤을 관리한다). 재빌드 후 브라우저에서
`getBoundingClientRect().right`가 창 폭과 같아지는 것을 실측 확인.

**"+ 새 프로젝트 만들기"가 편집기로 안 가던 것** — `2026-08-28` 결정이
이 단추를 이름부터 물어 기획(`plan`/`CreationInterview`) 화면으로 보내게
정했었는데, 캡컷은 이름을 물어도 이야기 화면 없이 편집기로 바로 간다.
이 단추 하나만 `AppRouter.tsx`의 `goToNewProject`가 빈 편집 세션을 만들어
편집기로 바로 가도록 되돌렸다 — `2026-08-28` 결정 전체를 뒤집는 게 아니라
이 버튼 하나에 대한 owner 재지시(2026-08-30)다. `빈 편집판으로 바로
시작`·`내 목소리 등록·클론` 두 지름길은 그대로다. `/create` 경로와
`CreationInterview` 자체는 안 지웠다 — 직접 그 URL로 들어가면 여전히 뜬다
(증거는 `app/AppRouter.test.tsx`의 "renders the durable Eugene creation
interview at the routed create leaf").

**유진 대화창이 탭으로 안 나뉘어 있던 것** — 이전 세션 지시가 실제로는
반영되지 않았고 배경만 검정으로 바뀌어 있었다. `RightDock.tsx`를
속성/유진/추천 세 탭(`role="tab"`)으로 나눴다 — 캡컷처럼 한 번에 하나만
보인다. 기억(memory) 패널은 따로 탭을 만들지 않고 유진 탭 안, 대화
다음 자리에 넣었다 — 대화와 기억 후보를 동시에 봐야 하는 흐름이 잦아서
탭을 오가면 오히려 더 불편했다. 탭이 전환될 때마다 대화 DOM이 완전히
마운트 해제되므로, 스크롤 복원 effect의 의존성 배열에 `pane`을 추가해야
했다 — 안 넣으면 유진 탭을 떠났다 돌아올 때마다 스크롤이 맨 위로
리셋되는 실제 회귀가 있었다(테스트로만 잡히는 게 아니라 진짜 버그).

**네이티브 컨트롤이 다크와 안 맞던 것** — `:root`에 `color-scheme: dark`
한 줄을 추가했다. 스크롤바·`<video>` 컨트롤 같은 브라우저/WebView2
네이티브 UI가 이 값 하나로 다크 대응 렌더링으로 바뀐다 — 기존 색상 값은
하나도 안 건드렸다.

이 네 가지 수정으로 RightDock을 쓰는 시험 파일 4개(`right-dock.test.tsx`,
`yujin-memory-panel.test.tsx`, `editor-workbench.test.tsx`,
`editor-workbench-route.test.tsx`, `AppRouter.test.tsx`)가 전부 깨졌다 —
거의 모든 테스트가 "세부 정보"를 연 뒤 바로 유진 대화창이나 추천 후보에
접근했는데, 이제는 탭을 먼저 눌러야 그 안의 내용이 DOM에 존재한다.
`task22-parity-owners.test.ts`의 증거 문자열 하나도 이름이 바뀐 시험
제목을 더 이상 못 찾아서 같이 고쳤다(위 참고). 재빌드(`--no-cache`) 후
브라우저에서 네 가지 전부 실측 확인: 스크롤바가 창 끝, `color-scheme:
dark` 적용, "+ 새 프로젝트 만들기" → 편집기 직행, 속성/유진/추천 세 탭
전환. 프런트엔드 전체 시험 1,371개, `tsc -b` 통과.

**정정(같은 날, 뒤이은 확인)**: 위 문단이 "BuildKit 캐시가 소스 변경을
못 본다"고 단정한 것은 재현하지 않고 내린 결론이었다. 뒤이어
`apps/web/src/ui-system.css`에 표식 주석을 붙이고 `docker compose build
videobox-workspace`(no-cache 없이)와 `owner-ready.ps1`이 실제로 쓰는
`... build --pull=false videobox-workspace`를 각각 직접 재현했더니
**두 번 다 `COPY apps/web ./`와 `RUN npm run build`가 캐시 없이 정상
재실행됐다** — `CACHED`가 안 떴다. 즉 이 세션에서 겪은 "재빌드해도
화면이 그대로였다"는 실제로는 BuildKit 레이어 캐시 문제가 아니라
[[videobox-container-rebuild-stale-bundle]]에 이미 적힌 **브라우저 쪽
번들 캐시**였을 가능성이 크다 — 둘 다 증상("다시 빌드했는데 화면이
그대로다")이 같아서 헷갈리기 쉽다. `--no-cache`로 우회한 것 자체는
결과적으로 문제없었지만, 그게 원인을 고쳤다는 뜻은 아니었다. 앞으로 같은
증상이 나오면 **Docker 캐시보다 브라우저 번들 해시 비교를 먼저** 본다.

### 7단계 완료 — 유진 대화창을 캡컷 EditPilot처럼 독립 패널로 (커밋 `21855f21`)

owner: "우리 유진 대화창도 캡컷처럼 해도 되." 6단계에서 만든 속성/유진/
추천 3탭 구조를 보낸 직후, 캡컷 실물 캡처를 추가로 받아 확인해 보니
(§7, 같은 파일) **캡컷 EditPilot은 애초에 속성 패널과 같은 도크의 탭이
아니었다** — 화면 구석에 늘 떠 있는 알약 버튼을 눌러야 따로 열리는
독립 패널이었다. 두 가지 구현 방식(구조를 바꿔 완전히 독립시키기 /
탭 구조는 유지하고 안쪽 디자인만 캡컷처럼) 중 owner가 **구조 변경**을
골랐다 — 이번 세션에 만든 3탭 구조를 다시 재작업하는 비용을 감수하고서.

**한 일**: `YujinPanel.tsx`를 새로 만들어 옛 "유진" 탭의 내용(대화 로그·
작성창·대화 스타터·완료 체크리스트·대본 추출 단추·편집안 다이얼로그와
미리보기·기억 패널)을 통째로 옮겼다. `RightDock`은 다시 두 탭(속성/추천)
으로 좁아졌고, 유진 전용 prop(`draft`·`messages`·`conversationScroll`·
`memory`·`editingProposal` 등)과 그 자동 재요청 효과도 함께 옮겼다.
`EditorWorkbench`가 `YujinPanel`을 `position: absolute`로 화면 구석에
띄우고, 열림 상태를 속성/추천 도크와 **완전히 별개로** 관리한다 — 도크를
닫아도 유진 대화는 그대로 남는다(예전엔 도크를 닫으면 유진 탭도 같이
사라졌다).

**시험**: `right-dock.test.tsx`·`yujin-memory-panel.test.tsx`·
`editor-workbench.test.tsx`·`editor-workbench-route.test.tsx`·
`AppRouter.test.tsx` 다섯 파일을 다시 고치고, 유진 대화 전용 시험 21개를
새 `YujinPanel.test.tsx`로 옮겼다. 라우트 시험 하나("도크가 언마운트돼도
route-owned 상태를 지킨다")는 기대값이 뒤집혔다 — 오른쪽 서랍을 닫아도
유진 대화가 남아 있는 게 **이제는 맞는 동작**이라 그렇게 고쳤다.
프런트엔드 전체 시험 1,373개, `tsc -b` 통과. 재빌드 후 브라우저에서
직접 확인: 도크는 정확히 두 탭만 보이고, 화면 구석의 "유진" 알약 버튼이
독립적으로 열고 닫히며, 속성 서랍을 닫아도 유진 패널은 그대로 열려
있었다.

**남긴 것 — 이 문단은 8단계에서 바로 뒤집혔다.** "추천 카드는 화면
공간이 더 필요해 작은 떠있는 패널에 안 맞는다"는 이 판단을 owner가
곧바로 반박했다: "그니까 내말이 그래서 캡컷도 화면공간이 필요해서
버튼들을 엄청 작게 만들었어. 그래서 나도 캡컷을 벤치마킹하라고 한거잖아."
공간이 모자라면 캡컷처럼 **작게 만들어서** 넣어야지, 별도 자리를 만드는
근거로 쓰면 안 됐다.

### 8단계 완료 — 추천 후보까지 유진 대화 로그 안으로 (커밋 `2a1013ea`)

owner 반박(위 7단계 끝 참고) 직후 바로 착수. 카드를 어디에 넣을지
두 선택지(대화 로그 안에 메시지와 섞기 / 로그 아래 별도 구역)를 확인해
**메시지와 섞기**로 정했다 — 캡컷 EditPilot이 실제로 그렇게 한다
(§6: 제안 세 개가 인사말 바로 다음, 대화 안에 있다).

**한 일**: `RightDock`을 다시 좁혔다 — 이제 `속성` 하나뿐이라 탭 줄
자체를 없앴다(탭이 하나면 탭이 아니다). `YujinPanel`이 제안 메타(기준·
현재 편집본, 찾은 방식)·일괄 고르기·후보 카드·적용 단추·검사 결과까지
전부 흡수해 대화 로그(`history`) 안, 완료 체크리스트 다음 자리에 둔다.
카드는 `.vb-yujin-panel__candidate` 전용 스타일로 폭 20rem 패널에
맞게 압축했다(글자 크기·여백만 줄이고 항목은 하나도 안 뺐다). "아직
추천이 없어요" 문구는 실제 제안이 있는데 후보가 0개일 때만 보이게
바꿨다 — 대화도 제안도 없는 첫 화면에서 "대화 시작 안 함"과 동시에
뜨면 중복이라서다.

**시험**: `right-dock.test.tsx`를 다시 속성 전용 4개로 줄이고, 추천
관련 20개 시험을 `YujinPanel.test.tsx`의 새 "추천 후보" describe로
옮겼다. `rightDockWiring.test.ts`의 ">3개 손잡이" 안전장치도 손잡이가
정말로 3개뿐인 지금 상태에 맞게 완화했다(0개 아니면 통과 — 정규식이
조용히 깨지는 사고만 막는다). 프런트엔드 전체 1,372개, `tsc -b` 통과.
재빌드 후 브라우저에서 확인: 속성 도크는 탭 없이 내용이 바로 보이고,
유진 패널을 열면 낡은 제안 경고·재요청 단추 다음에 제안 메타·후보
카드·적용 단추가 대화 로그 안에 그대로 이어져 보였다. 좁은 패널 안에서
카드가 겹치거나 넘치지 않았다(실측: 카드 폭 272px, 패널 346px, 가로
넘침 0건).
