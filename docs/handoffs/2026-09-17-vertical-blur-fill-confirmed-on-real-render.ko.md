# task_acfd8147 드디어 실물로 확인 -- 세로 배경 블러가 실제 렌더에서 제대로 나온다

`2026-09-17-vertical-fill-fit-generalized-to-padding-threshold.ko.md`에서
여백 크기 기준값으로 배경 채우기(검정/블러) 판단을 일반화한 뒤, **두
세션 연속으로 실제 렌더까지 못 가고** 막혀 있던 확인 항목이다. 막던
결함 둘(`task_4defd174`: 검토 승인이 재시도마다 사라짐,
`task_3dc11426`: 확인과 내보내기 화면에 충돌 해결 UI가 없음)이 이번
세션에서 풀려서, 드디어 실물로 봤다.

## 확인 방법

프로젝트 `0907-b26195af`(장면 2개, 94장면 ffmpeg 스레드 한도와 무관한
작은 프로젝트)의 `variant-editing_session_001-vertical_full`(제목 띠
없는 일반 세로, 여백 68.3% → 기대값 블러)을
`POST /variant-renders`로 렌더 → `succeeded`(1080x1920, 8.0초) →
`docker exec ffmpeg`로 2.0초/6.0초/7.5초 세 지점의 프레임을 뽑아
직접 봤다.

## 본 것

- 첫 장면(로봇 손·모니터 사진, 원본이 세로에 가까움): 위아래 아주
  얇은 회색 여백만 있고 나머지는 원본이 거의 꽉 채운다 -- 이 장면은
  여백이 작아 판단에 걸리지 않는 경우였다.
- 둘째 장면(발표 사진, 원본이 16:9에 가까움): **위아래 큰 여백이
  또렷하게 블러 처리된 배경**으로 채워져 있다 -- 검정이 아니라 같은
  사진을 확대·흐림 처리한 배경이다. 6.0초·7.5초 두 지점 모두 같은
  블러가 일관되게 나왔다.

기대(여백 68.3% → `_BLUR_PADDING_THRESHOLD` 50% 초과 → `blur`)와
실물이 정확히 일치한다. 지난 세션까지 쌓인 단위·통합 시험(138건, 정확한
26.9%/68.3% 수치 재현 확인)에 이어 **실제 렌더까지 확인이 끝났다.**

## 이번엔 코드를 안 건드렸다

순수 확인 작업이다. 시험·코드 변경 없음 -- 그래서 전체 pytest도 다시
돌리지 않았다(직전 커밋 `8ce1cc14`에서 이미 43분 49초짜리 단독 실행을
마쳤고, 이 문서는 그 위에 실물 확인 기록만 얹는다).

## 남은 백로그

1. `task_99becf89`(유진에게도 변형본 충돌 풀기를 열어 줄지) -- 판단
   필요, 대기 중.
2. `task_006f1523`(R1 경계 나누기) -- 여전히 대기, 안 급함 확인됨.
3. CapCut R2(세로 블러 육안 확인)는 원래 대표님이 사무실 데스크톱에서
   캡컷을 직접 열어야 하는 항목이라 이 세션에서 할 수 있는 게 아니다.

## 서버·환경 상태

- 컨테이너: 켜져 있음(healthy), 이번 확인은 기존에 떠 있던 컨테이너로
  했다(재빌드 불필요, 코드 변경 없음).
- worktree: `.worktrees/videobox-container-compatibility`, 브랜치
  `codex/videobox-container-compatibility`.
