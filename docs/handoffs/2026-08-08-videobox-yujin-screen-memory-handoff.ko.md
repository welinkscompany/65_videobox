# 2026-08-08 세션 핸드오프 — 승인한 기억이 처음으로 화면에서 쓰이기 시작한 날

## 한 줄 요약

유진의 기억 조회는 **한 번도 동작한 적이 없었다.** 게이트웨이 두 겹이 어댑터의 정상
응답을 해석하지 못했고, 실패 사유는 통째로 삼켜져 "기억 없음"으로만 보였다. 그걸 고친
뒤에도 owner 화면에서는 여전히 쓸 수 없었다 — **조회가 화면이 쓰지 않는 경로에만
붙어 있었기 때문이다.** 네 겹을 다 뚫고 나서야 편집 화면에서 한 바퀴가 돌았다.

## 지금 어디까지 되나 (화면 실측)

편집 화면에서 실제로 눌러서 확인했다. **헤르메스는 한 번도 쓰지 않았다.**

```
나   내 자막 배치 취향이 어떻게 되는지 숫자까지 그대로 알려 주세요.
유진 자막은 화면 아래에서 27% 지점에 배치합니다.
```

`27%`는 대화에 한 번도 등장한 적이 없고, 같은 화면의 `기억` 칸에 적어
`승인하고 저장`한 문장에만 있던 숫자다. 시험용 기억 두 개는 화면에서 지웠다.

## 막고 있던 것 넷 — 하나 고칠 때마다 다음 게 나왔다

### 1. 게이트웨이가 자기 어댑터의 답을 못 읽었다 (두 겹)

응답 모델이 `strict=True`인데 `memories`가 `tuple`이다. JSON은 언제나 배열(`list`)로
오고, strict 파이썬 모드는 list를 tuple로 받아주지 않는다. 매번 검증 예외 → 503 →
위층이 예외를 삼키고 빈 결과. **어댑터는 내내 정상이었다** — 200에 기억까지 담아
돌려주고 있었다.

- `memory_gateway.py`의 `HermesMemoryAdapterClient.search` (게이트웨이 → 어댑터)
- `agent_gateway_client.py`의 `search_memory` (API → 게이트웨이)

둘 다 이미 읽어둔 응답 **바이트**를 `model_validate_json`으로 해석하게 바꿨다.
`.json()`을 거치면 dict/list가 되어 strict 모드에 걸린다.

**테스트가 못 잡은 이유는 늘 같은 그것이다.** 검색 테스트가 전부 파이썬 객체를 그대로
돌려주는 가짜 어댑터를 썼다. HTTP JSON을 거치는 검색 테스트가 **한 개도 없었다.**

### 2. 화면 채팅 경로가 기억을 읽지 않았다

기억 조회는 `hermes_run_service`에만 배선돼 있었고, **`api.ts`의 `createHermesRun`을
부르는 화면이 없다.** 편집 화면의 `요청 보내기`는 로컬 우선 경로(`/messages`)로 간다
(owner 결정 `2026-08-05-local-first-assistant-decision.ko.md`).

`director_proposals.py`의 로컬 분기에서 `retrieve_approved_memories`를 부르고,
`YujinLocalConversationService.reply(memories=...)`가 프롬프트에 싣게 했다. 기억이
없으면 절 자체를 넣지 않는다 — 빈 제목은 모델이 취향을 지어내게 만든다.

### 3. 화면에서 나눈 대화로는 기억을 만들 수 없었다

`_completed_yujin_memory_source_rows`가 **완료된 헤르메스 run에 속한 메시지만**
기억의 재료로 인정했다. 로컬 경로에는 run 객체 자체가 없다(설계상 그렇다). 그래서
owner는 실제로 나눈 대화로 기억을 만들 수가 없었다.

완료된 로컬 턴 — `append_director_exchange`가 쓴 요청/응답 쌍 — 도 재료로 인정하게
넓혔다. **막을 것은 그대로 막았다:**

- 그냥 `append_director_message`로 들어온 메시지는 여전히 제외(`client_message_id`가 없다)
- 실패한 답변(`status: blocked`)도 제외 — 읽지 못한 메타데이터는 실패로 친다
- **어떤 run이든 소유한 메시지는 이 분기로 오지 않는다.** 안 그러면 진행 중이거나
  막힌 run이 "두 줄이 나란히 있다"는 이유만으로 통과한다

### 4. 그래도 버튼이 안 눌렸다 — 화면에 규칙이 한 벌 더 있었다

`EditorWorkbenchRoute.tsx`의 `completedDurableMemoryMessageIds`가 `hermes_run_id`를
요구하고 있었다. 서버 규칙과 같은 모양으로 맞췄다.

**교훈: 같은 규칙이 서버와 화면에 각각 있으면 한쪽만 고쳐도 사용자에게는 아무것도
바뀌지 않는다.**

## 컨테이너에서 유진 대화가 아예 안 되던 것도 함께 고쳤다

화면에 뜨던 문구: `local_qwen: Local Qwen request failed due to a transient network error`

`settings.py`가 로컬 모델 주소를 `http://127.0.0.1:1234/v1`로 **못박고 다른 값을
거부**하고 있었다. 컨테이너 안에서 `127.0.0.1`은 컨테이너 자신이고 LM Studio는
호스트에 있다. **컨테이너 모드에서는 구조적으로 두뇌에 닿을 수 없었다.**

`VIDEOBOX_LOCAL_RUNTIME_BASE_URL`을 두고 `host.docker.internal:1234` **하나만** 추가로
허용한다. 그 이름은 도커 호스트 — 같은 기계 — 를 가리키므로 "이 컴퓨터 밖으로 나가지
않는다"는 보장은 그대로다. owner 승인 2026-08-08, `§10.14` 조항 2-B에 기록했다.

## 알아둘 것

**기억은 대화 단위로 갇혀 있다.** 조회 SQL이 `WHERE conversation_id = ?`다. 다른
대화에서 승인한 기억은 설계상 절대 나오지 않는다. 앞선 인계의 "새 대화에서 확인하라"는
절차는 아무리 해도 통과할 수 없다. 넓힐지는 별도 결정이다.

**유진은 기억 후보를 제안하지 않는다.** `videobox-memory/SKILL.md`가 "대화 내용을 자동
수집·저장하지 않는다"고 못박고 있고, owner가 `기억` 칸에 직접 적는 것이 의도된 설계다.
앞선 인계의 "후보 제안 흐름 확인" 항목은 전제가 틀렸다.

**컨테이너를 다시 빌드하면 브라우저가 옛 번들을 잡고 있다.** 화면이 이상하면
제품 결함으로 보기 전에 주소창에 아무 쿼리나 붙여 강제로 다시 받아 본다. 이번에
`유진 기억` 패널이 통째로 사라진 것처럼 보였는데 원인이 이것이었다.

## 계획서 장부를 실제와 맞췄다

- **Task 27** — 모든 Step이 완료인데 제목만 `미착수`였다 (커밋 `645d0bbee`)
- **Task 31** — 제목이 `미착수 · 최우선`인데 실제로는 닫혀 있었다 (커밋 `fd5bec945`).
  `TimelineReviewPage.tsx:140`이 `api.approveTimeline`을 부르고 `검토 승인` 버튼이 있다
- **Task 34 Step 1** — 죽은 데이터 재확인을 마쳤다. 표는 계획서에 있다

## 정리한 데이터 (owner 승인 2026-08-08)

- `runtime\projects\projects\` 중첩 잔재 — `_intake_probe.mp4` 30.4MB. 어떤 자산도
  가리키지 않는 고아였고, SHA-256이 제자리 사본·라이브러리 원본과 같은 것을 확인하고 지웠다
- `비롤_라이브러리` — `runtime\`과 `65_videobox-project\`에서 지웠다 (1KB 스텁)

**지우지 않은 것 둘, 이유가 중요하다.**

- `snapshot\비롤_라이브러리` — 스냅샷 매니페스트가 **모든 파일을 해시로 고정**한다.
  지우면 `verify_container_snapshot`이 실패하고 **API가 아예 안 뜬다**
- `tts-sample\chatterbox-korean-sample.wav` — 계획서 2105줄의 "들어보실 파일"이자
  **Task 25가 기다리는 owner 청취 대상**이다. 조사 문서에는 "코드 참조 0건"으로
  적혀 있었으나 계획서와 owner 테스트 안내서가 가리키고 있다

## owner 결정이 남은 것

1. **촬영본 726MB의 자리** — 로컬 실행 쪽 폴더에 있어 컨테이너에서 안 보인다.
   `docs/videobox-data-path-audit.ko.md` §5 참조
2. **Task 25 TTS 목소리** — 샘플을 직접 들으셔야 한다
3. **Task 26 마이크 실측** — owner 참관 필요
4. **기억을 대화 밖으로 넓힐지** — 위 "알아둘 것" 참조

## 홈 대시보드도 실제 상태를 말하게 했다 (Task 35 완료)

owner가 "대시보드가 전부 완성돼야 테스트할 수 있다"고 해서 이어서 진행했다. 홈은
카드 세 개의 문구를 **전부 무조건 단정**하고 있어서 셋 다 거짓일 수 있었다 —
완성본을 만들어도 "아직 없어요"가 그대로 떴다.

**막고 있던 제약을 뒤집지 않고 우회했다.** `ProductShell.test.tsx`가 "작업 목록은
`작업 상태`를 열 때만 부른다"를 의도적으로 고정하고 있다. 그래서 홈이 작업 목록을
부르는 대신 `GET /api/projects/{id}/home-summary` **하나만** 부르고 세는 일은 서버가
한다. 홈 방문당 요청 한 번은 그대로다.

요약을 못 읽으면 **상태를 단정하지 않는 원래 문구로 돌아가고** 카드는 계속 눌린다.
세지 못하는 홈이 이동까지 막아서는 안 된다.

브라우저 실측으로 하드코딩이 아님을 확인했다 — `b-roll-smoke-test`는
`완성한 영상이 1개 있어요`, `progress-bar-live-test`는 `완성한 영상이 2개 있어요 /
아직 시작한 작업이 없어요`.

## B-roll 분석이 컨테이너에서 처음으로 돌았다 (같은 세션, 나중에)

owner가 퇴근하며 "테스트할 수 있게 해두라"고 해서 이어서 진행했다.

`VIDEOBOX_MEDIA_ANALYSIS_ENABLED`가 **compose에 없어서** 컨테이너는 계속
`_UnavailableMediaAnalysisService`로 떨어졌다. Task 19에서 코드로는 활성화했는데
컨테이너 설정으로 옮기지 않은 것이다 — **LM Studio 주소와 똑같은 종류의 누락**이다.

그리고 켜도 안 됐을 것이다. `lm_studio.py`의 `LMStudioHTTPTransport`가
`127.0.0.1:1234`를 **따로 한 번 더 못박고** 있었다. 대화 경로는 앞에서 풀었지만
비전·임베딩 경로는 그대로였다. **같은 결함이 한 층 위에 하나 더 있었다.**

- 허용 호스트를 `127.0.0.1`과 `host.docker.internal` 둘로 좁게 열었다
- **네이티브 모델 목록 주소도 같은 호스트를 따라가게** 했다. 이게 별도 상수로
  박혀 있어서, 안 고치면 컨테이너에서 조용히 아무것도 없는 곳에 물어본다
- `main.py`가 대화 경로가 쓰는 주소를 그대로 비전 전송에 넘긴다. 두 곳이
  갈라지면 한쪽만 되는 상태가 또 생긴다

**owner 실제 촬영본으로 확인했다.** `20260323_152916.mp4`를 가져오기 →
분석이 `queued` → `running` → **`succeeded`**. 결과에 실측값이 들어 있다:
1920x1080 hevc 11.7초 aac, 그리고 비전 모델이 장소·조명·정적 장면을 문장으로
기술했다. 예전에는 이 자리가 `MEDIA_ANALYSIS_WORKER_UNAVAILABLE`이었다.

**worker를 켜니 잠자던 결함이 하나 드러났다.** worker가 없던 시절에 만들어진 분석
기록은 **프로필 행이 없다.** 그런데 dispatch가 그것을 엄격하게 조회해서, owner가
`다시 분석하기`를 눌러도 매번 내부 문구 `Media analysis profile not found`로 실패했다.
**그 기록은 영원히 실패 상태를 벗어날 수 없었다.** 새 enqueue가 쓸 프로필로 폴백하고
그것을 기록해 두도록 고쳤다. 7월부터 막혀 있던 실제 기록이 재시도로 **성공**했다.

**분석을 켠 것이 유진 추천까지 같이 살렸다.** 이건 예상하지 못했다.
`build_director_proposals_router`가 임베딩 제공자를 `app.state.media_analysis_embedding_provider`
에서 받는데, 그 값은 **분석이 켜져 있을 때만** 채워진다(`main.py:580`). 그래서 컨테이너에서는
`_apply_semantic_scores`가 늘 조용히 어휘 매칭으로 폴백하고 있었다 — Task 20에서 붙인
의미 검색이 실제로는 한 번도 쓰이지 않았다는 뜻이다.

전 구간 실측(2026-08-08):

| 단계 | 결과 |
|---|---|
| 가져오기 | `20260323_152916.mp4` → `asset_c713d390abd9` |
| 분석 | `queued` → `running` → `succeeded` |
| 임베딩·장면 구간 | 각 2건 기록됨 |
| 유진 추천 | 후보 4개, **오늘 가져온 클립이 두 구간 모두에 올라옴** |
| 의미 점수 | 폴백 경고 없음 = 어휘 매칭이 아니라 실제 의미 점수가 적용됨 |

**촬영본 9개(726MB)를 컨테이너 라이브러리로 복사했다**(원본은 그대로 뒀다).
이제 `GET /api/media-inbox/assets`가 9개를 돌려준다.

## 내일 테스트할 때 알아둘 것 — 추천이 정해지는 시점

**B-roll 추천은 초안을 만들 때가 아니라 `준비` 단계에서 정해진다.** 준비 결과에
`broll_candidates`가 들어 있고, 초안 만들기는 그 목록을 그대로 쓴다.

그래서 **이미 있는 준비 기록으로 초안을 다시 만들면 새로 가져온 촬영본은 안 뽑힌다.**
결함이 아니라 그대로 다시 만드는 성질이다. 실제로 확인해 봤더니 이 프로젝트의 준비
기록은 2026-07-18자였고, 그날 기준 후보만 들어 있었다.

**새 촬영본을 쓰려면 `새 영상 만들기`부터 시작한다.** 그러면 새 준비 기록이 생기고
그때 추천이 다시 계산된다.

owner 경로를 화면에서 한 번씩 다 열어 확인했다(2026-08-08).

| 화면 | 상태 |
|---|---|
| 홈 | 실제 개수 표시 + 유진 대화창 |
| 새 영상 만들기 | 정상, `유진과 기획 시작` |
| 자산 | 촬영본 9개, 가져오기·분석 동작 |
| 편집 | 유진 대화 + 기억 저장·회수 |
| 검토 | `검토 승인` 버튼 있음 |
| 출력 | 완성본 1개 |

## 검증

- 백엔드 회귀 전체, 프론트 771 passed, `tsc` 통과
- 화면 실측: 로컬 대화 → 기억 저장 → 승인 → 다시 질문 → `27%` 회수 → 기억 삭제
- 컨테이너 5개 healthy

See `docs/videobox-data-path-audit.ko.md`,
`docs/handoffs/2026-08-08-videobox-yujin-runtime-and-local-memory-handoff.ko.md`.
