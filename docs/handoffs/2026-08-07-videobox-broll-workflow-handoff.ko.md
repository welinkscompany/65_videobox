# 2026-08-07 핸드오프 — B-roll 작업 흐름 완성과 남은 owner 결정

`codex/videobox-container-compatibility`, worktree clean, 커밋 12건.

## 한 줄 요약

owner의 실제 작업 방식(폰 촬영 → Drive → B-roll로 활용)이 이 세션에서 처음 드러났고,
그에 맞춰 **촬영본을 프로젝트로 가져오고 → 쓸 구간을 자동 추천하고 → 사람이 고치는**
경로를 끝까지 연결했다. 계획서 Task 1~21은 TTS 하나만 남기고 이미 완료 상태였다.

## 이 세션에서 닫은 것

| Task | 내용 | 커밋 |
|---|---|---|
| 22 | 가져오기를 `raw_video` → `broll_video`로 정정 | `82355e743` |
| 23 | 긴 촬영본에서 쓸 구간 선택 | `acc816e48` |
| 27 | 실제 장면 경계 감지 (placeholder 교체) | `645d0bbee` |
| 28 | 장면 구간을 비전 모델 성공 여부와 분리 | `f5470cf63` |
| 29 | 비전 타임아웃을 영구 실패가 아닌 재시도로 | `2a2a68b5f` |
| 24 | 편집 화면에서 B-roll 구간 손보기 | `07f3f3145` |
| 25 Step 1 | TTS 한국어 샘플 생성 (격리 환경) | `bf7d1fa2b` |

**owner 실제 영상 9개 전부에서 추천이 개선됐다.** 8분 편집본은 컷 12개를 잡아 6분 지점의
안정된 장면(346.6s)을 고르고, 짧은 폰 클립 8개는 카메라 세팅 구간을 건너뛴 2.0~7.0s를
고른다. 이전에는 예외 없이 "앞에서 5초"였다.

## owner 결정이 필요한 것 (3건)

1. **TTS 목소리 판정 (Task 25 Step 2).** 샘플을 들어보고 쓸지 정한다.
   `20_project\65_videobox-project\tts-sample\chatterbox-korean-sample.wav`
   기본 목소리로 만든 것이며, **본인 목소리 복제는 10초짜리 참조 녹음이 필요하다.**
   CPU에서 실시간의 4배가 걸리므로 본격 사용에는 GPU 빌드가 사실상 필수인데,
   LM Studio가 이미 GPU를 쓰고 있어 VRAM 경합을 따져야 한다.
2. **직접 녹음 실제 마이크 실측 (Task 26).** 구현은 이미 있고 테스트도 있지만
   **실제 마이크로 전사까지 도달한 기록이 없다.** owner의 1순위 경로이므로
   함께 한 번 돌려봐야 한다. 마이크 권한과 실제 발화가 필요해 무인 실행이 불가능하다.
3. **편집 화면 B-roll 구간 조절 눈으로 확인 (Task 24).** 렌더·동작·전송 본문·백엔드
   보존을 각각 테스트로 고정했지만, 이 패널에 도달하려면 실제 초안 세션이 필요해
   브라우저 실측은 하지 않았다. 다음에 초안을 한 번 만들 때 확인하면 된다.

## 남은 기술 부채 (owner 결정 불필요)

- **Task 30 — `exact_preview` 테스트의 시계 경쟁 조건.** `older_than_seconds=0`을
  실제 시계로 판정해 간헐적으로 실패한다. 변경 없이도 실패하는 것을 stash로 확인했다.
  **회귀를 못 믿게 만드는 것이 문제**라 우선순위가 낮지 않다.
- **Task 29의 남은 절반.** 버려진 요청이 LM Studio를 계속 점유하는 것 자체는 그대로다.
  재시도로 완화했을 뿐이고, 점유를 없애려면 요청 취소나 동시성 제어가 필요하다.
- **TTS 반입 시 `setuptools<81` 고정 필수.** `resemble-perth`가 제거된
  `pkg_resources`를 쓴다. 고정하지 않으면 모델 초기화가 실패한다.

## 이 세션에서 내가 틀렸던 것 (다음 세션이 반복하지 않도록)

정직하게 남긴다. 세 번 다 **한 번의 측정으로 단정**한 것이 원인이었다.

1. **착수도 안 한 Task를 `[x]`로 표기했다.** 바로 되돌렸지만, owner가 "완료된 것만
   완료로 표기하라"고 한 바로 그 지점에서 정반대 실수를 했다.
2. **"장면 구간이 이미 계산되고 있다"고 보고했다.** 저장 호출만 보고 생성부를 확인하지
   않았다. 실제로는 길이 전체를 감싼 placeholder였다.
3. **`LM_STUDIO_BLOCKED`의 원인 진단이 세 번 바뀌었다.**
   타임아웃 부족 → 내가 돌린 회귀의 CPU 포화 → 대기열 점유.
   두 번째까지는 오염된 환경에서 잰 값을 근거로 삼았다.

**교훈:** 부하 조건을 통제하고 반복 측정하기 전에는 원인을 쓰지 않는다.
그리고 호출부만 보고 데이터가 존재한다고 단정하지 않는다.

## 검증 환경 함정 2건

- **`preview_start`는 worktree가 아니라 main 체크아웃에서 서버를 띄운다.**
  worktree에서 직접 `scripts/run_api.py`를 실행해야 한다.
- **포트 8000이 이전 서버에 잡혀 있으면 새 서버가 조용히 뜨지 않고 요청이 옛 코드로 간다.**
  이번 세션에서 실제로 겪었고, 그럴듯한 결과가 나와서 하마터면 믿을 뻔했다.

## 최종 검증 상태

- 백엔드 회귀 **3,106 passed / 52 skipped**, 실패는 Task 30(기존 불안정) 1건
- 프론트엔드 **764 passed**, `tsc --noEmit` 통과
- 실제 API 서버 + 브라우저로 Task 22 역방향 검증 완료
- 실제 LM Studio로 Task 27·28 역방향 검증 완료
