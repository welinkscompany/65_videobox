# 찍어 둔 영상으로 시작하는 문이 화면까지 닿았다

- 작성: 2026-08-21
- 앞 문서: `2026-08-21-videobox-scene-images-and-the-donkey.ko.md`
- 개발선: `codex/videobox-container-compatibility`
- 이 턴의 커밋: `1dda575cc`

## 한 줄

하루 전에 놓인 백엔드 문(`7ed84d040`)에 **부르는 화면을 붙였다.** 이제 owner가
첫 화면에서 "찍어 둔 영상이 있어요"를 눌러 영상을 올리면, 받아쓴 대본이 고칠 수
있는 칸에 나오고, 확인하면 기존 기획 흐름으로 넘어간다.

## 무엇을 했나

- 첫 화면 선택창(`StartChooser.tsx`)에 세 번째 길을 넣었다.
- 이야기 화면에 올리는 자리를 만들었다(`features/creation/SourceVideoStart.tsx`, 새 파일).
  올림 → 받아쓰는 동안 상태 표시 → **고칠 수 있는 칸** → 확인 → `createCreationBrief`.
- 대본을 만들어 준 그 영상을 **내레이션 후보(`raw_video`)로도 이어 붙였다.**
- `api.ts`에 `uploadSourceVideo`(FormData)를 더했다.
- `ProductShell.tsx`를 건드렸으므로 `docs/oss/editor-ui-source-map.json`의
  `normalized_sha256` 두 줄도 함께 옮겼다.

## 실제로 재서 알게 된 것

**받아쓰기는 생각보다 훨씬 빠르다.** 3분 20초짜리 영상이 **24초**에 끝났다
(대략 8배속, 잡음 없는 한 사람 목소리 기준). 처음에 화면 안내를 "5분 안쪽"이라고
**짐작으로** 적었는데, 재 보니 근거가 없어 "128MB보다 작고 20분 안쪽"으로 고쳤다.
중간에서 끊는 벽은 330초다(`docker/workspace-nginx.conf`).

**받아쓰기는 실제로 틀린다.** 검증 중에 "받아써서"가 `받았어서`로 나왔다.
고칠 칸을 두는 설계가 장식이 아니라는 증거다. 그대로 확정했으면 그 오타가
자막까지 그대로 갔다.

**`raw_video` 내레이션 단추는 이번에 처음으로 그려졌다.** 그리는 코드
(`CreationInterview.tsx:520`)와 후보를 읽는 API(`listDraftNarrationOptions`)는
전부터 있었지만, `raw_video` 후보를 **만들 방법이 아예 없어서** 한 번도 화면에
나온 적이 없었다. 이 저장소가 반복해 온 "부품은 있는데 부르는 자리가 없다"의
또 한 번이고, 이번에 닫혔다.

## 검증한 것

컨테이너를 다시 띄우고(`scripts/owner-ready.ps1 -Mode Start -Rebuild
-WithYujinMemory`, 전 항목 PASS) **브라우저에서 owner 경로를 그대로 밟았다.**
말소리가 든 영상을 직접 만들어 올렸다(Windows SAPI 한국어 음성 + ffmpeg).

- 첫 화면에 세 번째 길이 보이고, 누르면 이야기 화면으로 간다
- 17초 영상 → 받아쓴 한국어 대본이 textarea에 나옴 (정확)
- 그 글을 **고친 뒤** 확인 → 고친 글로 기획이 열림 (받아쓴 원문이 아니라)
- 승인 뒤 "영상 소리로 초안 준비"가 뜨고, 누르면 실제 요청이
  `narration_choice: {kind: "source_video", asset_id: "asset_ecd15ac2ad55"}`
- 무음 영상 → "이 영상에는 말소리가 없어요…"
- `.txt` 파일 → "열 수 없는 형식이에요…" (서로 다른 말)
- 3분 20초 영상 → 기다리는 동안 "영상에서 말을 받아쓰고 있어요" + 대기 안내가
  뜨고 단추가 잠긴다. **그 상태에서 다시 눌러도 요청이 두 번 가지 않았다**(1건)
- `npm --prefix apps/web test -- --run` 1207개 전부 통과, `npx tsc --noEmit` 깨끗
- `test_editor_ui_source_provenance.py` + `test_api_source_video_start.py` 26개 통과

## 검증하지 못한 것

- **330초 벽을 실제로 넘겨 보지 못했다.** 8배속이면 45분짜리 영상이 있어야 하는데
  만들지 못했다. 504를 받았을 때 나올 문구는 **되짚기에서만** 확인했고 실물
  프록시로는 확인하지 못했다. 긴 영상이 실제로 504로 오는지, 아니면 다른 방식으로
  끊기는지는 미확인이다.
- **128MB 상한을 실제로 넘겨 보지 못했다.** 413/400 문구도 되짚기까지만이다.
- **화면 스크린샷을 찍지 못했다.** Browser pane이 화면에 떠 있지 않아 접근성
  트리도 비어 있었다. 대신 `document.body.innerText`와 DOM 조회로 확인했으므로
  **문구와 동작은 실제로 확인했지만, 생김새·배치·간격은 확인하지 못했다.**
  세 번째 단추가 선택창 안에서 어떻게 보이는지는 사람이 한 번 봐야 한다.
- **긴 영상의 업로드 시간**은 재지 않았다. 받아쓰기 시간만 쟀다. 330초 예산은
  올리는 시간과 받아쓰는 시간을 **함께** 쓴다.

## 다음 사람이 알아야 할 것

- **지금 도는 컨테이너는 이 에이전트 worktree에서 빌드된 것이다.** 개발선 branch
  (`codex/videobox-container-compatibility`)에는 아직 이 커밋이 없다. 합친 뒤
  개발선 worktree에서 다시 `owner-ready.ps1 -Mode Start -Rebuild`를 돌려야
  컨테이너와 개발선이 일치한다.
- 검증용 프로젝트 두 개를 만들어 두었다: `project-15ed00b1`(찍어둔 영상 시작 확인),
  `project-04180302`(실패 문구 확인). 지우지 않았다 — 지우기는 owner 몫이다.
- 남은 길은 하나다: **유진이 처음부터 대본을 써 주는 길.** 선택창에서 일부러
  빼 두었고, 그 이유는 `StartChooser.tsx` 주석과 되짚기에 적혀 있다.

## 껍데기를 캡컷 배치로 — 부품은 섰고, 아직 안 끼웠다

owner가 2026-08-21에 왼쪽 기둥을 없애고 위 띠 하나로 가기로 했다
(`docs/decisions/2026-08-21-capcut-shell-layout.ko.md`). 이유가 둘이고 성격이 다르다 —
**지금** owner가 아는 배치라 바로 쓸 수 있고, **나중에** 구독으로 팔면 처음 켠 사람이
설명 없이 쓸 수 있는 것이 제품값이 된다.

**된 것:** `apps/web/src/features/shell/TopBar.tsx`와 시험 6개(`top-bar.test.tsx`),
그리고 `product-shell.css`의 `.vb-top-bar*` 규칙. 단계 이동·프로젝트 전환·설정이
전부 들어 있고, 프로젝트는 지금 것만 보이고 나머지는 눌러야 나온다.

**안 된 것: 아직 `ProductShell`에 끼우지 않았다.** 끼우는 순간 왼쪽 기둥이 없어지는데,
그건 `ProductShell.tsx` 115~180줄의 껍데기 블록을 통째로 들어내는 일이고 사이드바를
참조하는 시험이 여럿 걸려 있다. 남은 여유로 시작하면 중간에 끊길 위험이 커서 멈췄다.

**다음 사람이 할 일:**

1. `ProductShell.tsx`의 `<Sidebar>…</Sidebar>`(125~178줄)를 들어내고 그 자리에
   `<TopBar />`를 놓는다. `SidebarProvider`/`SidebarInset`도 같이 정리한다 —
   기둥이 없으면 하는 일이 없다.
2. **기둥을 참조하는 시험부터 읽어라.** `ProductShell.test.tsx`가
   `data-slot="sidebar"`, `작업실 접기`, `프로젝트 전환`, `프로젝트 단계`를 잡고 있다.
   2026-08-17에 배운 것 그대로 — **값이 아니라 그 시험이 무엇을 지키려는지** 보고 다시 쓴다.
3. 기둥에 있던 것이 **조용히 사라지지 않았는지** 확인한다. `top-bar.test.tsx`가
   단계·프로젝트 전환·설정 셋을 이미 묶어 두었다. 기둥을 없애는 변경에서 가장 흔한 사고다.
4. e2e 스냅샷이 전부 낡는다. owner 확인이 필요하다(`2026-08-15` 절차).

**같이 넘기는 것:** owner가 말한 진짜 문제는 색도 배치도 아니었다 —
*"어떤 버튼을 눌러야 할지 하나도 모르겠어."* 첫 화면을 세어 보니 누를 수 있는 것이
50개쯤이고 그중 다음에 할 일로 보이는 것이 다섯 개였다. 선택창(`StartChooser`)이
그 다섯을 하나로 줄인 조각이고, 위 띠는 그 위에 얹는 조각이다. **순서가 뒤바뀌면
껍데기가 아무리 캡컷 같아도 owner는 여전히 어디부터 눌러야 할지 모른다.**

### 위 띠 끼우기를 한 번 시도했다가 되돌렸다 — 다음 사람은 이걸 먼저 보라

2026-08-21에 `ProductShell`의 사이드바를 들어내고 `TopBar`를 끼워 봤다. 타입 검사는
통과했고 `top-bar.test.tsx`도 초록이었는데, **껍데기 시험 34개 중 16개가 깨졌다.**
그중 7개가 같은 원인이었고 그게 이 시도의 진짜 교훈이다.

**왼쪽 기둥의 프로젝트 스위처는 "프로젝트를 고르는 곳"이 아니라 프로젝트 관리 화면
전체였다.** 그 54줄 안에 이것들이 들어 있다.

- 이름 바꾸기(`ProjectTitleDialog`)
- 보관하기 / 영구 삭제(확인 두 번)
- 보관함 목록과 되돌리기
- 그 동작들이 실패했을 때의 재시도 안내

기둥을 들어내면 **이것들이 통째로 갈 수 없는 곳이 된다.** `TopBar`의 스위처는
프로젝트를 고르는 것만 하므로 그대로는 대체가 안 된다. 아이러니하게도 이 인계의
앞부분이 "기둥에 있던 것이 조용히 사라지는 사고"를 경고하고 있었는데, 그 경고를
쓴 뒤에 그대로 밟았다. **`top-bar.test.tsx`가 지키는 셋(단계·전환·전체 메뉴)만으로는
부족했다** — 지켜야 할 것이 넷째로 하나 더 있었다.

**owner가 정했다 (2026-08-21): 프로젝트 관리는 `프로젝트` 화면으로 옮긴다.**

띠는 **고르는 것만** 맡고, 이름·보관·삭제·보관함은 목록 화면에서 한다. 관리는 자주
하는 일이 아닌데 띠에 매달려 있을 이유가 없고, 띠가 가벼워야 캡컷처럼 보인다.

**다음 사람이 할 일 — 이 순서를 지켜라. 뒤집으면 아까처럼 중간에 깨진다.**

1. ~~**먼저 `프로젝트` 화면을 완성한다. 껍데기는 그 다음이다.**~~ **닫혔다 (2026-08-21).**
   `ProjectCatalogCard`에 보관하기(확인 한 번), 영구 삭제(확인 두 번), 그리고 목록
   화면 아래에 보관함과 되돌리기가 들어갔다. 실패 시 재시도 안내도 같이 간다.
   컨테이너를 다시 띄워 **실제로 시험용 프로젝트 하나를 보관하고, 보관함에서
   되돌리고, 영구 삭제까지 해 봤다** — 서버 상태로 확인했다.

   **복사가 아니라 옮기기로 했다.** 확인을 몇 번 받는지 같은 규칙은
   `apps/web/src/features/projects/projectManagement.ts`의 `useProjectManagement()`
   하나에 있고, 기둥과 목록 화면이 **같은 것**을 쓴다. 화면에 보이는 문구는
   일부러 각 JSX에 그대로 두었다 — `src/user-copy-policy.test.ts`가 JSX만 읽으므로
   단추 이름을 상수로 빼면 창작자 문구 검사가 두 화면에서 한꺼번에 꺼진다.

   `ProductShell.tsx`를 건드렸으므로 `docs/oss/editor-ui-source-map.json`의
   `normalized_sha256` 두 줄도 함께 옮겼다.

2. ~~**이제 바로 이것을 하면 된다.** `ProductShell`의 `<Sidebar>…</Sidebar>`를 들어내고
   `TopBar`를 끼운다.~~ **닫혔다 (2026-08-21).** 아래 `### 기둥을 들어냈다` 절 참고.
3. ~~시험 16개를 고친다.~~ **닫혔다 (2026-08-21).** 같은 절에 어느 것을 왜 지웠는지 적었다.

**되돌린 것은 `ProductShell.tsx` 하나뿐이었다.** `TopBar.tsx`, `top-bar.test.tsx`,
`.vb-top-bar*` CSS는 그대로 남아 있었고, 이번 턴에 그 위에 끼웠다.

### 관리 옮기기에서 확인하지 못한 것 (2026-08-21)

- **생김새·간격은 못 봤다.** Browser pane이 화면에 떠 있지 않아 viewport가 `0x0`이고
  접근성 트리가 비어 온다. `resize_window`로도 안 바뀐다. 문구·상태·서버 결과는
  DOM 조회로 실제 확인했지만, **카드 아래 관리 단추 줄과 보관함이 보기에 어떤지는
  사람이 한 번 봐야 한다.**
- **기둥의 데스크톱 모습으로는 못 밟았다.** viewport가 `0x0`이라 껍데기가 모바일
  분기를 타서 사이드바가 Sheet로 그려진다. 기둥의 보관 확인 단계가 그대로 도는 것은
  그 Sheet를 열어 확인했다(`제목 바꾸기 · 보관하기 · 완전 삭제` → `보관 확인`,
  누른 뒤에도 서버는 그대로). 데스크톱 폭에서의 기둥은 확인하지 못했다.
- **e2e 스냅샷은 다시 만들지 않았다.** `product-shell.spec.mjs`가 찍는 곳은
  `/projects/<id>/home`이고, 이번 변경은 `/projects` 목록 화면에만 단추를 더했다.
  `ProductShell`의 JSX는 상태 배선만 바뀌어 그려지는 결과가 같다. **낡지 않았다고
  판단했을 뿐 돌려서 확인하지는 않았다.**
- 곁다리로 본 것: 프로젝트 이름을 한글로 지으면 `project_id`가 한글을 통째로
  버린다. `관리화면 점검 A` → `a`. 이번 변경과 무관한 옛 동작이라 건드리지 않았다.

### 기둥을 들어냈다 — 이제 위 띠 하나다 (2026-08-21)

`ProductShell.tsx`에서 `<Sidebar>…</Sidebar>`와 `SidebarProvider`/`SidebarInset`을
통째로 들어내고 `<TopBar>`를 놓았다. **머리말(`.vb-product-header`)도 함께 없앴다** —
프로젝트 이름·작업 상태가 띠와 머리말 두 곳에 겹쳤고, 띠 위에 머리말을 또 얹으면
세로 7.5rem을 먹어 "기둥을 없애 화면을 넓힌다"는 뜻이 사라진다. 작업 상태 다이얼로그는
`TopBar`의 `children`으로 들어갔다.

**기둥에 있던 것이 어디서 지켜지는지 — 하나씩 짚었다.**

| 기둥에 있던 것 | 지금 있는 곳 | 지키는 시험 |
|---|---|---|
| 단계 넷(이야기·재료·편집·확인과 내보내기) | 띠 | `top-bar.test.tsx`, `ProductShell.test.tsx`(단계 넷·아이콘) |
| 프로젝트 전환 | 띠(지금 것만 보이고 나머지는 눌러야 나옴) | `top-bar.test.tsx`, `ProductShell.test.tsx` |
| 전체 목적지 넷 | 띠의 접힌 메뉴 | `top-bar.test.tsx`, `ProductShell.test.tsx`, `AppRouter.test.tsx`(세 화면 모두) |
| 이름 바꾸기·보관·영구 삭제·보관함·재시도 안내 | `프로젝트` 목록 화면 | `AppRouter.test.tsx` 6개 |
| 브랜드(`VideoBox`) | 띠 왼쪽 끝 | (시험 없음, 예전에도 없었음) |
| `이 기기에서 작업` 표시 | 띠 오른쪽(`children`) | (시험 없음, 예전에도 없었음) |
| 머리말의 **화면 이름** | 띠 — 단계가 없는 화면에서만 | `top-bar.test.tsx` 2개(새로), `AppRouter.test.tsx` |

**화면 이름은 일부러 조건부다.** 단계 단추가 켜져 있으면 그것이 이미 "여기가 어디인지"를
말한다. 옆에 이름을 또 적으면 owner를 막았던 그 문제 — 화면에 그럴듯한 것이 하나 더
늘어나는 것 — 를 되풀이한다. 그래서 `내 라이브러리`·`촬영본 정리`·`설정`·`프로젝트`처럼
**단계로 표시되지 않는 화면에서만** 나온다. 이 넷 중 앞의 둘에는 보이는 제목이 아예
없어서, 이게 없으면 화면 이름이 통째로 사라진다.

**지운 시험 12개 — 왜 지웠는지.**

- *의미 자체가 사라지는 것 (5개)*: 접힘 상태(`data-state="collapsed"`), 접었을 때의
  아이콘 전용 이름, 가져온 부품의 영어 문구(`Toggle Sidebar`), `작업실 접기` 키보드
  접근, 사이드바 구획 라벨(세 구획이 세로로 쌓일 때만 뜻이 있었다). 기둥이 없으면
  지킬 대상이 없다.
- *`프로젝트` 화면 시험이 같은 것을 지키는 것 (7개)*: 보관·이름 바꾸기·삭제 2단
  확인·실패 재시도·보관함 열기/되돌리기·빈 보관함. 전부 `AppRouter.test.tsx`에
  대응이 있다. **다만 하나는 정확히 일치하지 않는다** — 되돌리기가 실패했을 때의
  안내는 목록 화면 쪽에 따로 없고, 보관 실패 시험이 같은
  `useProjectManagement().run` 경로를 지난다. 규칙이 한 곳(`projectManagement.ts`)에
  있어서 실질적으로 덮이지만, 별개 시험은 아니다.

**다시 쓴 시험**: 전체 목적지 넷과 단계 넷의 구분, 프로젝트가 없을 때 단계를 안
보여 주는 것, 단계 아이콘, 프로젝트 전환, 홈이 행동만 담는 것,
`AppRouter.test.tsx`의 세 화면 메뉴·화면 이름·설정 진입.

**CSS도 같이 정리했다.** `.vb-product-sidebar`, `.vb-global-nav`, `.vb-project-switcher`,
`.vb-product-nav`, `.vb-sidebar-*`, `.vb-mobile-menu`, `.vb-collapse`,
`.vb-product-header`, 좁은 화면 Sheet 규칙을 지웠다(전부 없어진 마크업만 가리킨다).
대신 `.vb-top-bar__brand/__screen/__note/__menu/__menu-list` 규칙을 넣었다 —
**`__menu`와 `__menu-list`는 규칙이 아예 없었다.** 그대로 뒀으면 전체 메뉴를 열 때
목록이 띠 안으로 밀고 들어가 띠가 통째로 벌어졌을 것이다.

`components/ui/sidebar.tsx`는 **지우지 않았다.** 출처 핀이 걸려 있고
(`docs/oss/editor-ui-source-map.json`), 지우면 provenance 게이트가 깨진다. 지금은
아무도 import하지 않는다.

**남긴 판단 하나 (owner 몫):** `ProductShell.tsx`의 머리말 주석과 source map이
아직 `app-sidebar.tsx`를 출처로 적고 있다. 그 코드는 이제 이 파일에 없다. 저작권
표기를 줄이는 일이라 임의로 지우지 않았다.

**확인한 것과 못 한 것**은 아래 `### 위 띠 끼우기 검증` 절에 적었다.

### 위 띠 끼우기 검증 (2026-08-21)

`scripts/owner-ready.ps1 -Mode Start -Rebuild -WithYujinMemory`로 다시 띄우고
**브라우저에서 실제로 밟았다.** 전 항목 PASS.

**확인한 것 (1440x900):**

- 띠에서 단계 넷을 눌러 각 화면이 실제로 떴다 — `이야기`→`/create`(기획 화면),
  `재료`→`/media`(자산 화면), `편집`→`/editor`(편집 작업판이 실제로 그려짐),
  `확인과 내보내기`→`/review`(검토·출력). 누를 때마다 그 단추만
  `aria-current="page"`가 됐다.
- 프로젝트 전환: 지금 것만 보이고, 누르면 나머지 15개가 뜨고, 고르면 그 프로젝트의
  편집기로 갔다. 고른 뒤 목록이 닫혔다.
- 전체 메뉴 넷이 전부 살아 있다. `내 라이브러리`→`/library`, `촬영본 정리`→`/footage`,
  `설정`→`/settings/general`(누르면 메뉴가 닫힌다), `프로젝트`→`/projects`.
- 화면 이름: `/projects`에서 `홈`, `/library`에서 `내 라이브러리`, `/footage`에서
  `촬영본 정리`, `/settings`에서 `설정`. 단계가 켜진 화면에서는 **나오지 않는다**(중복 없음).
- 작업 상태 다이얼로그가 띠에서 열리고 유진 연결 상태·작업 목록이 그대로 나온다.
- 레이아웃: 띠 69px, 본문이 바로 아래 69px에서 시작, 가로 넘침 없음
  (`bodyScrollWidth == innerWidth`), 편집 작업판 767px에 세로 스크롤 없음.
- 전체 메뉴 목록이 `position:absolute`로 띠 **아래에 걸린다** — 띠를 밀지 않는다.

**재다가 고친 것 두 개 (이 턴에 같이 넣었다).**

1. **375px에서 단계 칸이 32px로 눌렸다.** 띠를 한 줄로 두면 브랜드·프로젝트·작업
   상태·메뉴가 폭을 다 먹고, `flex:1 1 auto`인 단계 칸만 줄어든다. 네 단추가
   사실상 못 누르는 상태였다. 기둥 시절에는 이 폭에서 Sheet가 그 일을 했으므로
   **여기서 잃으면 안 된다.** 좁은 화면에서 띠를 두 줄로 접고 단계에 제 줄을 줬다.
2. **편집판 높이가 어긋났다.** `calc(100svh - 4.25rem)`의 `4.25rem`은 옛 머리말
   높이였고, 띠가 두 줄이 되는 좁은 화면에서 페이지가 32px 밀려났다. 띠 높이를
   `--vb-top-bar-h`로 빼서 두 파일이 같은 값을 쓰게 했다.
   고친 뒤 375px에서도 `bodyScrollHeight == innerHeight`다.

**확인하지 못한 것:**

- **생김새는 못 봤다.** Browser pane이 화면에 떠 있지 않아 스크린샷이
  `Screenshot timed out ... not compositing frames`로 실패한다. 구조·문구·크기·위치는
  접근성 트리와 `getBoundingClientRect`로 실제로 쟀지만, **"보기에 캡컷 같은가"는
  owner가 봐야 안다.** 특히 띠의 여백·정렬·브랜드 크기, 좁은 화면에서 두 줄이 된
  띠의 모양.
  (`resize_window`는 이번에는 **동작했다** — 앞 인계의 "안 바뀐다"는 더 이상 맞지 않는다.
  뷰포트를 1440x900·375x812로 실제로 바꿔 가며 쟀다.)
- **e2e 스냅샷은 다시 만들지 않았다.** 이번에는 확실히 낡았다 — 껍데기가 통째로
  바뀌었다. `apps/web/e2e/snapshots/README.ko.md`가 **사람이 차이를 검토한 뒤에만**
  다시 쓰라고 못박고 있어서 손대지 않았다. 다섯 폭(1920/1440/1280/768/390) 전부
  owner 확인이 필요하다(`2026-08-15` 절차).
- **`product-shell.spec.mjs`는 기둥 부분만 고쳤다.** 그 파일에는 **이번 변경과 무관한
  옛 assertion이 남아 있다** — `다음 작업`, `영상 만들기 시작`,
  `프로젝트 만들고 소스 등록`, `자산 준비하기`. 전부 `apps/web/src`에 더는 없는
  문구다(`StartChooser`가 들어오면서 없어졌다). 이 e2e는 **내 변경 이전에 이미**
  통과할 수 없는 상태였다. 범위 밖이라 건드리지 않았다.

### 에이전트 worktree에서 backend 전체 pytest를 돌리면 42개가 깨진다 (환경 문제)

전체 pytest를 단독으로 돌렸더니 `3873 passed, 42 failed`였다. **42개 전부
PowerShell 스크립트를 부르는 시험이고, 원인은 이 worktree에 `.venv`가 없는 것이다.**

- `test_start_hermes_yujin_script.py` 31개
- `test_hermes_yujin_profile_distribution.py` 5개
- `test_smoke_hermes_yujin_creator_flow_script.py` 6개

스크립트들이 `<repositoryRoot>/.venv/Scripts/python.exe`를 못박고 있어서
(`scripts/start-hermes-yujin.ps1:110`, `scripts/verify-hermes-yujin-profile.ps1:33`),
없으면 `... runtime is unavailable`로 죽는다. **재서 확인했다** — worktree에 빈
`.venv`를 만들고 `pyyaml`·`cryptography==45.0.6`을 넣자 **42개가 5개로 줄었고**,
남은 5개도 `ModuleNotFoundError: No module named 'httpx'`였다. 확인 뒤 그 `.venv`는
지웠다.

**즉 프런트 변경과 무관하다.** 프런트 소스를 실제로 읽는 backend 시험
10개 파일(`test_editor_ui_source_provenance.py` 포함) **247개는 전부 통과**했다.

다음 사람에게: 이 worktree에서 backend 전체를 돌릴 거면 `.venv`를 먼저 만들고
`requirements-dev.txt`를 설치해라. 안 하면 초록이어야 할 42개가 빨갛게 나온다.
