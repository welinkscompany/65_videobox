# 2026-08-29 인계 — ComfyUI 실측 검증, 코드리뷰 결함 수정, 캡컷 구조·다크 전환 시작

## 이번 세션에서 실제로 한 일

### 1. 음성 녹음·자동채우기·유튜브 학습 3기능 — 코드리뷰 + 갭검증 + 역방향 검증 + QA

- 8개 각도 코드리뷰로 확실한 결함 4건 발견·수정·검증 완료(커밋 `bc8aa915e`):
  - `autoFillRemainingGapsWithAi`가 실패해도 "채우는 중" 문구가 안 지워지던 것
  - 유튜브 재생목록 링크(`watch?list=...&v=...`) 오인식
  - ffprobe 경로를 문자열 치환으로 만들던 잠재 결함
  - 유튜브 입력 오류가 500으로 새던 것 → 422로 수정
  - 자동채우기 무한루프 방지용 stall guard 추가
- 백엔드 전체 pytest 4136 passed, 프론트 전체 1362 passed 둘 다 통과 확인.
- **설계 판단이 필요해 남겨 둔 것 3건** (다음에 결정 필요):
  - 유튜브 학습: 다운로드 최대 10분 허용인데 분석(컷·색감)은 3분 제한 — 긴 영상이면 분석 단계에서 이유 불명확한 실패가 날 수 있음
  - 유튜브 학습 전체 과정이 요청 하나 안에서 동기로 돎 — 아주 긴 영상이면 nginx 프록시 330초 타임아웃보다 오래 걸릴 수 있음
  - 자동채우기 도는 중 수동으로 다른 장면을 만들면 `draft_readiness_revision_conflict`로 부딪힐 수 있음

### 2. ComfyUI를 실제로 켜고 처음부터 끝까지 검증

- owner가 직접 `C:\Users\atgro\Documents\comfy\ComfyUI\.venv\Scripts\python.exe main.py --listen 0.0.0.0 --port 8188`로 켜서 컨테이너(`host.docker.internal:8188`)에서 닿는지부터 확인.
- **진짜 결함 발견**: libx264가 호스트 CPU 16코어만큼 스레드를 쓰려다 컨테이너 CPU 할당량(2코어)과 부딪혀 `pthread_create() failed: Resource temporarily unavailable`로 죽음. `-threads 2`로 고정해 해결(커밋 `6ce7b51db`).
- **실물 증거**: 대본만 있는 프로젝트 → "빈 장면 모두 AI로 채우기" → 그림 3장 생성 → 검토 승인 → 완성본까지 실제로 만들어 **15초·1920×1080 mp4**를 owner에게 전달함. AI 영상 생성 기능(자동 채우기 + 검토 + 렌더)이 owner 컴퓨터(RTX 5090)에서 끝까지 실제로 동작함을 확인.
- owner가 "AI 영상 생성"을 묻길래 정정함: 지금 만든 건 **정지 화면 확대/축소(zoompan)**이지 진짜 동영상 생성(힉스필드·클링류)이 아니다. 2026-08-28 결정으로 "AI 영상 생성은 범위 밖"이라고 이미 정해져 있었다는 것도 같이 알림. **다음 세션에서 방향(클라우드 API vs 로컬 비디오 모델) 결정 필요.**

### 3. 캡컷 메뉴·구조 대조 감사

- 2026-08-21·2026-08-27에 이미 승인된 "편집기 중심화 5단계 계획"의 실제 이행 상태를 감사:
  - 1단계(이름 통일) 거의 됨, 2단계(미디어 3페이지 → 편집기 도크 탭) 안 됨, 3단계(내보내기·검토 팝업화) 절반만(내보내기만 팝업), 4단계(첫 화면 정리) 됨, 5단계(옛 이름 정리) 거의 됨.

### 4. owner가 캡컷 캡처 두 장을 보여주며 방향을 확장

- 1차 캡처: 캡컷 첫 화면(배너 "+ 프로젝트 만들기" → AI 진입 카드들 → 최근 프로젝트 그리드). owner가 VideoBox 첫 화면도 이렇게/비슷하게 만들고 싶다고 함.
- 2차 캡처: 캡컷 편집 작업판, 패널 드래그 크기조절을 보여주며 "웹에서는 이런 게 안 되니 설치형으로 가자"고 주장.
  - **이 전제를 실측으로 반박함**: VideoBox 편집 작업판도 이미 `react-resizable-panels`(shadcn/ui 표준)로 마우스 드래그 크기조절을 갖고 있다. 자동화 도구 한계로 완전히 재현은 못 했지만 코드 구조상 실제 드래그를 받는 게 맞고, owner가 직접 브라우저에서 눌러보면 확인 가능하다고 안내함.
  - owner에게 실측 결과를 전달했고, **설치형(Electron/Tauri) 전환은 이번 세션에서 승인하지 않음** — owner도 구조·색상 작업부터 보고 다시 판단하기로 함.
- 이어서 owner가 "캡컷과 구조 전체 동일화 + 다크 테마"를 명시적으로 확정(AskUserQuestion으로 범위 확인 — "구조+색상까지"로 답함), "SSOT 문서부터 다 갱신해"라고 지시함.

### 5. SSOT 문서 갱신 (커밋 `1cfd8048a`, `84c05176`)

- 새 결정 기록 작성: `docs/decisions/2026-08-29-capcut-full-structure-and-dark-theme.ko.md`
  - `2026-08-21-editor-back-to-light.ko.md`를 되돌림(그 문서 51행이 요구한 "대체하는 새 승인 기록").
  - `2026-08-21`·`2026-08-27`의 "따라가는 건 껍데기뿐" 구분 중 **색 부분만** 뒤집음(구조 쪽 "캡컷에 없는 기능은 안 만든다" 원칙은 유지).
  - 다크 팔레트 값 제안 — 대비 기준(4.5:1) 실측 검증 완료:
    - canvas `#0F0F11` / panel `#18181B` / 옅은 패널 `#202024`
    - 텍스트 `#F2F2F3`(15.84:1) / 보조 `#A3A3AC`(7.08:1) / 희미 `#8A8A93`(5.18:1, 원래 제안값 `#7A7A82`는 4.16:1로 미달이라 조정)
    - 강조 `#EA580C`(4.98:1, 원래 브랜드값 `#C2410C`는 다크 배경에서 3.42:1로 미달이라 한 단계 밝힘)
    - 미리보기 `#0B0B0C`, 성공 `#4ADE80`/`#0F2A18`
  - **설치형 패키징은 이 문서로도 승인 안 됨** — 별도 확인 필요라고 명시.
- `CLAUDE.md` §2 결정 목록 갱신, `2026-08-05`·`2026-08-21-editor-back-to-light` 문서 상단에 "대체됨" 표시 추가(역사 기록 유지, 삭제 안 함).

## 백그라운드 에이전트가 진행한 것 (같은 세션, 별도 프로세스)

owner의 "2·3번 마저 끝내줘" 지시로 캡컷 구조 5단계 계획의 2·3단계 구현을 백그라운드 에이전트에 맡김. 세션 종료 시점까지 확인된 커밋:

- `80fe82170` — feat: keep review inside the export popup instead of navigating away (3단계, 검토 팝업화). 실물 확인 완료: 내보내기 팝업 안에 "영상 검토"가 그대로 뜨고, 팝업 안 링크가 화면을 못 떠나게 바뀜.
- `5511fa702` — fix: rename the last "내 라이브러리" labels to "미디어" (1단계 마무리 + 2단계 재확인)

**세션 종료 직전에 마무리 지시를 보냈고, 에이전트가 정상 완료 보고를 남기고 끝났다.**
결과 — 두 단계 다 완료:

- **2단계는 사실 이미 끝나 있었다.** 감사 당시 "/library`·`/footage`가 여전히 독립
  페이지"라고 판단한 게 **낡은 판단**이었다 — 2026-08-27 커밋(`0fa0a89d4`~`9938ee9b0`,
  이번 세션 이전에 이미 이 브랜치에 있던 것)이 도크 폭(220~400px)에 전체 페이지가
  안 들어가서, 페이지 자체를 도크에 넣는 대신 `AddMediaFiles`·`VoiceMaterialPanel`·
  `ImportFromFootageInbox`·`LibrarySidebar`(`LibraryPickerDialog`로 팝업화)를
  재사용해 이미 도크 안에 넣어 뒀었다. 에이전트가 재사용 게이트 확인 중 이걸
  찾아내고 **중복 구현하지 않았다** — 올바른 판단이다. 진짜 남아 있던 건 "내
  라이브러리" 잔여 라벨뿐이었고 그건 이번에 "미디어"로 정리했다.
- 프론트 전체 테스트: **97개 파일 · 1363개 통과, 실패 0**.
- 백엔드는 손 안 댐(파이썬 파일 미변경) — 전체 pytest 안 돌림, 필요 없음.

다음 세션 시작 시 확인만 하면 된다:

```bash
cd .worktrees/videobox-container-compatibility
git log --oneline -10
git status --short
```

현재(2026-08-29 세션 종료 시점) 기준 작업 트리는 깨끗하고 전부 푸시돼 있다.

## 2·3단계 코드리뷰 결과 (마무리 직전에 추가로 진행)

owner가 "코드리뷰·갭검증·역방향·기능검증 안 해도 되냐"고 물어서 8개 각도 코드리뷰를
따로 진행했다. 확실한 결함 2건은 바로 고쳤고, 나머지는 결정/후속 작업으로 남겼다.

**바로 고친 것 (커밋 예정):**
- `apps/web/src/styles/product-shell.css` — 완성본 4칸 그리드가 내보내기 팝업
  (35rem 폭)에도 그대로 적용돼 칸마다 169px로 눌려 "완성본" 카드가 넘치는 것을
  실측으로 확인(전체 페이지에선 268px라 안 넘침) → 팝업 안에서만 1칸으로.
- `editor-workbench.test.tsx` — "검토 화면 열기" 링크가 되살아나도 잡아낼 시험이
  없었다 → 부재를 확인하는 assertion 추가.

**결정이 필요해서 남긴 것:**
- **이름 충돌.** 프로젝트 단계 탭의 "미디어"(프로젝트 자산), 편집기 도크 탭의
  "미디어"(같은 화면에 동시에 보임, 실측 확인), 전체 메뉴의 "미디어"(구
  `/library`, 프로젝트 넘나드는 공용 라이브러리) — 이름 통일(1단계)만 끝내고
  2단계(도크 탭 통합)를 다른 방식으로 우회한 결과, 정확히 2026-08-27 문서가
  경고했던 "한 화면에 같은 이름 여럿" 문제가 다른 모양으로 다시 생겼다. 셋이
  범위가 실제로 다르므로(프로젝트 전용 vs 편집기 안 vs 프로젝트를 넘나드는 공용),
  이름을 억지로 하나로 맞추는 대신 owner가 세 자리의 관계를 어떻게 부를지
  결정해야 한다.

**참고만 하고 안 고친 것 (심각하지 않음, 다음에 여유 있을 때):**
- 팝업 안에서 장면 클릭 시 타임라인 클립 선택이 완전히 안 풀리는 경우가 있을 수
  있음(추정 — 재현 못 함, 코드 경로상 가능성만 확인)
- 장면 찾기 로직이 파일 안에 세 벌 — 하나에만 있는 규칙(내레이션 우선순위)이
  나머지 둘엔 없음
- 내보내기 팝업을 열 때마다 상태 조회가 두 번씩 나감(비효율, 기능 문제 아님)
- 팝업 안에 `<h1>` 두 개·`aria-live` 두 개가 겹침(스크린리더 경험, 기능 문제 아님)

프론트 전체 테스트: 1363 passed, 0 failed (이 수정들 반영 후 재확인).

## 2026-08-29 이어진 세션 — 위 여섯 항목 처리

같은 날 자율 모드로 이어서 진행. owner가 AskUserQuestion으로 답한 결정들과 실제
구현·검증 결과다.

### 1. 다크 테마 — 코드로 구현·검증 완료 (커밋 `f452da74b`)

`apps/web/src/ui-system.css`의 `:root`를 승인된 다크 값으로 교체
(`--vb-canvas: #0F0F11`, `--vb-panel: #18181B`, `--vb-text: #F2F2F3` 등),
`contrast.test.ts`를 다크 팔레트 대비 검증으로 재작성(9건 통과),
`verify-editor-ui-system.ps1` 방향을 다크 토큰 요구·흰 토큰 거부로 뒤집었다.
컨테이너를 재빌드해 브라우저에서 실제 렌더링 색을 `getComputedStyle`로 확인 —
canvas `rgb(15,15,17)`, panel `rgb(24,24,27)`, text `rgb(242,242,243)` 전부
일치. (이 세션은 `computer{action:"screenshot"}`이 안 먹혀 스크린샷 대신
계산된 스타일로 검증했다 — 다음 세션은 스크린샷도 같이 남길 것.)

### 2. 첫 화면 카드형 재구성 (커밋 `ee67ed70`)

`+ 새 프로젝트 만들기` 배너는 그대로 두고(2026-08-22 결정 유지 — 대본부터
시작하는 대표 진입로는 하나뿐), "빈 편집판으로 바로 시작"·"내 목소리 등록·클론"
두 지름길만 아이콘 카드(Scissors/Mic)로 바꾸고 `aria-describedby`로 한 줄
설명을 달았다. 목록 위에 "최근 프로젝트" 제목을 추가했다. **VideoBox에 없는
기능(AI 이미지·동영상 카드 등)은 자리를 만들지 않았다** — 실제 있는 지름길만
카드로 놓는다는 2026-08-29 결정 원칙을 지켰다. 컨테이너 재빌드 후 브라우저에서
그리드·아이콘·설명 텍스트 렌더링 확인 완료.

### 3. AI 영상 생성 방향 — 로컬 비디오 모델로 결정 (커밋 `9e276bac9`)

owner가 로컬 비디오 모델(ComfyUI 확장)을 선택. **방향 승인일 뿐 구현 미착수**다 —
`docs/decisions/2026-08-29-ai-video-naming-and-packaging-followups.ko.md`에
남은 조사 항목(기존 ComfyUI 영상 생성 워크플로 유무, 리소스 실측, nginx
타임아웃 대비)을 적어 뒀다. **다음 세션이 여기부터 시작해야 한다.**

### 4. 설치형 패키징 — 보류 유지, 결정 없음

owner가 "보류 유지"를 선택. 코드 변경 없음. 위 문서에 재확인만 기록.

### 5. 코드리뷰 설계 판단 3건 — 전부 처리

- 유튜브 학습 분석 시간 제한을 다운로드 한도(600초)에 맞춰 늘림(커밋 `da15d697`).
- 유튜브 학습 전체를 비동기로 전환 — `BackgroundTasks` + `job_id` 폴링
  (커밋 `4029f9baf`). 작업 상태는 SQLite가 아니라 오케스트레이터 메모리에만
  있다 — 재시작하면 진행 중 작업이 사라지는 트레이드오프를 감수했다(일회성
  작업이라 재시도 비용이 낮다고 판단).
- 자동 채우기 도는 동안 수동 장면 편집(건너뛰기·구간 저장) 단추를 잠금(커밋
  `da15d697`).

### 6. "미디어" 이름 충돌 — 전체 메뉴만 "자료실"로 (커밋 `9e276bac9`)

프로젝트 단계 탭·편집기 도크 탭의 "미디어"는 그대로. 전체 메뉴의 공용
라이브러리(구 `/library`)만 이름을 바꿨다. 실제 화면에서 브레드크럼·전체
메뉴·페이지 이름표까지 확인 완료.

### 부수적으로 고친 것

- `CLAUDE.md`가 8571자로 `test_entry_point_stays_short_enough_to_actually_carry_weight`의
  8000자 한도를 넘겨 실패 — §4·§6의 오래된 장문 문단을 뜻은 안 잃게 줄여서
  7999자로 되돌림(커밋 `7a657643`).
- `ProductShell.tsx`(자료실 이름 변경으로 내용이 바뀜)의 `normalized_sha256` 핀이
  `docs/oss/editor-ui-source-map.json`에서 낡아 `test_editor_ui_source_provenance.py`
  5건이 실패 — 새 해시로 갱신(같은 커밋). **UI 소스를 고치면 이 핀도 같이
  갱신해야 한다**는 기존 메모가 다시 확인됐다.

## 다음 세션에서 할 일 (우선순위 순)

1. **AI 영상 생성(로컬 비디오 모델) 실제 조사·구현 시작.** 방향은 정해졌으니
   ComfyUI에 이미 있는 영상 생성 워크플로 확인 → 리소스/시간 실측 →
   nginx 타임아웃 대비 설계(유튜브 학습 비동기 전환 패턴 재사용 가능한지 검토).
2. **다크 테마·첫 화면 재구성의 스크린샷 증거 남기기.** 이번 세션은 계산된
   스타일로만 검증했다 — 다음 세션에서 스크린샷 도구가 되면 실제 화면도 남길 것.
3. **설치형 패키징 여부** — 구조·다크 작업이 다 끝났으니 이제 다시 논의할 시점.

## 검증 상태

- 백엔드 전체 pytest: 이어진 세션 최초 실행에서 CLAUDE.md 길이·provenance 해시
  드리프트로 6건 실패 → 둘 다 고치고 **재실행 4136 passed, 56 skipped, 0 failed**
  (`0:34:27`).
- 프론트 전체 vitest: 1363 passed, 0 failed (이어진 세션의 모든 변경 반영 후).
- 다크 테마·첫 화면 재구성: 컨테이너를 세 번 재빌드하며(다크 테마 →
  이름 변경 → 첫 화면 카드 → 비동기 전환) 매번 브라우저에서 계산된 스타일과
  DOM으로 실물 확인. 새 `GET .../from-youtube/{job_id}` 엔드포인트도 브라우저
  fetch로 404 응답 확인(라우팅이 실제로 붙었다는 증거) — 스크린샷은 이번 세션
  도구 제약(Browser pane 비표시)으로 못 남겼다.
- 유튜브 학습 비동기 전환: 백엔드 단위 테스트(`test_api_reference_style_import.py`)로
  확인 — 실제 유튜브 영상으로 긴 다운로드가 202를 즉시 돌려주는지는 아직
  실물 확인 못 함(다음 세션 몫).
- 커밋: `f452da74b`(다크 테마) · `9e276bac9`(자료실 이름·AI 영상 방향 기록) ·
  `ee67ed704`(첫 화면 카드) · `da15d697e`(타임아웃·자동채우기 잠금) ·
  `4029f9baf`(유튜브 학습 비동기) · `7a657643`(CLAUDE.md 길이·provenance 해시).
  전부 이 worktree 브랜치에 커밋됨, push는 안 함(요청 없었음).
