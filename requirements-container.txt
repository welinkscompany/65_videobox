fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.20
psycopg[binary]==3.2.9
cryptography==45.0.6

# Real speech-to-text and CapCut draft export.  Without these the container
# falls back to MockSTTProvider and a null exporter, so captions and segments
# come from fixed placeholder text rather than the actual audio.
# Versions match requirements-runtime.txt.
faster-whisper==1.2.1
pycapcut==0.0.3

# owner 요청(2026-08-29): 본인 유튜브 영상에서 목소리 샘플·편집 스타일 참고를
# 뽑아 오는 길. 본인 소유 영상만 대상이고, 이 패키지 없이는 그 길이 아예 없다
# (그 자리에서 명확한 오류로 안내한다).
yt-dlp==2026.8.19

# ComfyUI websocket(`/ws`)의 `executing` 이벤트를 실시간으로 받아, 장면 영상
# 취소가 다른 작업을 잘못 멈추는 TOCTOU 경합을 없앤다(코드리뷰 2026-08-30).
websockets==17.1
